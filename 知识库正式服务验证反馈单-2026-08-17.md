# 统一知识库正式服务验证反馈单

- 反馈方：企业智能打标系统（bqxt）
- 验证日期：2026-08-17
- 验证地址：`https://platform-enterprise.yicall.com/kb-api`
- 契约依据：《经验库对接接口文档-约定基准版》（2026-08-14 双方确认版，下称"基准版"）
- 结论：**服务已可达、§5 只读面接口形态正常；但租户维度实现与基准版不一致，属阻断性问题，当前无法开展联调。**

---

## 一、验证通过项

| 接口 | 结果 |
| --- | --- |
| `GET /dimensions` | 200，返回 4 个全局维度 |
| `GET /knowledge-bases/{kb}/enabled-dimensions` | 200，kb=1~5 均正常 |
| `GET /knowledge-bases/{kb}/stats` | 200，kb=1~5 均正常 |
| `GET /knowledge-bases/{kb}/knowledge-points?coord=...`（携带已启用维度） | 200，返回知识点及 resolved 答案，解析形态符合 §6 |
| 未启用维度的 coord 查询被拒绝 | HTTP 400，符合基准版 §5 约定 |

当前已建库观测：kb=1（3 个知识点，内容为"苹果/梨"等测试数据）、kb=2（86 点，抽样内容为打标经验）、kb=3（23 点）、kb=4（8 点）、kb=5（1 点，启用全部 4 维度）。

---

## 二、阻断性问题（需对方处理后才能联调）

### 问题 1：租户维度 key 与基准版不一致，我方全部查询被拒绝

- **基准版约定（§3）**：租户隔离用英文 **`tenant`** 维度（text 型）表达；单租户经验 `coord={"tenant": "..."}`，全局经验 `coord={}`；我方（对接方）**所有读取强制携带非空 `tenant` 条件**，这是双方确认的红线，也是规避 §6 fallback-latest 风险的手段。
- **实际观测**：正式服务 `GET /dimensions` 返回的维度为 `人员`、`分类`、`场景`、`行政区划`，**不存在 `tenant` 维度**。
- **复现**：

  ```
  GET /knowledge-bases/2/knowledge-points?coord={"tenant":"测试"}
  → HTTP 400 {"code":444, "msg":"维度 tenant 未在本知识库启用"}
  ```

- **影响**：我方按红线携带 `tenant` 条件的每一次经验读取都会被拒绝，打标经验注入、AI 小助手规则库读取全部不可用（我方侧按设计降级为无经验运行）。
- **需对方处理**：在全局维度中新建 **`tenant`**（key=`tenant`，text 型），并在对接涉及的知识库中启用。若贵方坚持以中文 `行政区划` 作为租户维度，属于对基准版 §3 的契约变更，需双方书面重新确认后我方再改造适配——但请注意问题 2 表明改名并不能解决问题。

### 问题 2：三个正式库均未启用租户维度，存量数据不具备租户隔离条件

- **实际观测**：kb=2 / kb=3 / kb=4（合计 117 个知识点，应为迁移过去的三个业务库）`enabled-dimensions` **均只启用了 `分类` 一个维度**，连 `行政区划` 都未启用。
- **推论**：这三个库的存量答案均未携带任何租户条件（抽样验证 kb=2 答案 coord 为按 `分类` 组织）。
- **影响**：这是基准版 §12 明确要防的核心风险——**租户 A 的经验会被注入租户 B 的打标上下文**。基准版 §12.1 约定"每条经验必须写 `tenant` 条件，除非在全局经验白名单内"；§12.3 约定 tenant 漏写/错写属录入侧责任。当前数据形态下，即使接口层修好问题 1，租户隔离在数据层面仍不成立。
- **需对方处理**：
  1. 在 kb=2/3/4 启用 `tenant` 维度；
  2. 对 117 个存量知识点逐条确认归属：属于某租户的答案补写 `tenant` 条件，确属全局共享的列入全局经验白名单（白名单清单请同步我方备案）；
  3. 完成后我方按基准版 §12.3 跑一次 tenant 巡检脚本（基于 `answer-groups` 接口）核验。

---

## 三、非阻断问题（请一并确认）

### 问题 3：`GET /knowledge-bases`（库列表）返回 401，预留 token 无效

- **实际观测**：`GET /knowledge-bases` 无论是否携带我方预留 token（`Authorization: Bearer a8a6…`）均返回 HTTP 401（`"登录已过期，请重新登录"`）。
- **说明**：该接口不在基准版 §5 签字的免鉴权只读面内，我方页面已按设计降级为手动填写 kb_id，不阻断使用。
- **请对方确认**：是否将该接口纳入只读面（我方经验配置页可直接下拉选库，体验更好）；若纳入，采用免鉴权还是 token 方案，token 如何发放。

### 问题 4：kb=1 疑似测试数据残留在正式环境

- kb=1 含"苹果""梨"等测试知识点。请确认该库用途；若为调试库请告知我方避开，或考虑清理。

---

## 四、需对方提供的信息

1. **三个业务库的 kb_id 对应关系**（我方需写入配置）：
   - 打标经验库 → kb_id = ？（观测疑似 kb=2）
   - AI 小助手规则库 → kb_id = ？
   - 运营技能库 → kb_id = ？
2. 全局经验白名单清单（对应问题 2 第 2 项）。
3. 正式环境的变更窗口/发布通知渠道（基准版 §11 兼容性承诺相关）。

---

## 五、我方侧状态（无需对方动作，仅同步）

- 我方已完成基准版全部对接改造（coord 化查询、TTL 缓存与降级、任务/明细两级留痕快照、经验配置管理），本地全链路已验证通过。
- 我方所有读取严格走 §5 只读面，不写入贵方服务。
- 上述问题 1/2 解决后，我方**零改动**即可开始联调；期望对方给出处理排期。
