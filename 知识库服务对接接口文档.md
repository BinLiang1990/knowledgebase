# 知识库服务 · 对接接口文档

面向对接方（前端或第三方系统）。本文档根据后端代码（`backend/src/kb_backend`）逐接口核对生成，
是 Swagger/OpenAPI 自动文档之外的**叙述版**说明——重点补充 Swagger 不会讲的东西：字段之间的
依赖关系、状态机、解析算法、每个 400 错误的具体触发条件。字段名、路径、状态码如与 Swagger
冲突，以代码 / Swagger 为准，请提 issue 更新本文档。

对接的核心能力是**按维度条件查询知识点的答案**（下称"条件查询"）：给一个知识库、一个知识点、
一组"维度=取值"条件和一个查询时间点，返回命中的那条答案。除此之外，知识库/知识点/答案本身的
增删改接口也一并开放，供第三方做数据迁移或直接管理内容。

**没有 Mock 数据，也没有回调**：所有请求由你方发起，服务端不会主动回调你方任何地址。

---

## 0. 现状特别提示：暂无鉴权

**当前版本所有接口不设鉴权**（无 API Key / 无签名 / 无 Token）。这是产品文档里明确写下的阶段性
决定，不是遗漏：鉴权方案（API Key / 内网白名单 / 其他）留待后续单独设计，v1 阶段"接口先放开"，
不阻塞对接。

- `CORS` 只挡浏览器发起的跨域请求（默认允许 `http://localhost:5173`），**对服务器到服务器的调用
  没有任何限制**——只要能访问到部署地址，就能调用任意接口；
- 也没有租户/数据隔离机制。**"租户"在这个系统里只是一个普通维度**（`key=tenant`，文本类型），
  和其他维度走同一套增删改查逻辑，并不提供数据库级隔离；
- 没有操作人身份系统。所有写操作的 `operator` 字段 v1 阶段固定写入 `"admin"`，字段结构保留了未来
  接入外部身份系统的空间，但目前不区分"谁做的"；
- 没有请求签名、没有 nonce、没有频率限制。

**接入前请与我方确认部署环境的网络边界**（是否放在内网、是否有网关层鉴权），公网直接暴露前建议
先补上访问控制。

---

## 1. 核心概念

| 概念 | 说明 |
|---|---|
| 知识库（Knowledge Base） | 顶层容器，`status` 为 `active`/`deprecated`。知识点、答案都归属于唯一一个知识库 |
| 维度（Dimension） | 全局定义一份的"条件字段"，如 `地区`、`租户`、`客户等级`。字段：`key`（同时是主键与展示名，创建后不可改）、`field_type`（`text`/`number`/`date`/`boolean`）、`weight`（1~100，权重回退用）、`status`（`active`/`deprecated`） |
| 知识库启用维度 | 每个知识库从全局维度里勾选一个子集；**只有本知识库启用且全局仍是 `active` 的维度**，才能出现在这个知识库的答案条件、查询条件里 |
| 知识点（Knowledge Point） | 一个可被问到的"问题"，如"退货政策"。同一知识库内 `title` 唯一。`status` 为 `active`/`deleted`（软删除） |
| 答案（Answer） | 知识点在某个"条件组合"下的一份具体说法：内容 `content`、生效时间 `effective_time`、条件 `coord`、来源 `source`、是否撤回 `revoked`。同一知识点下**不同条件组合的答案彼此独立、永久并存**，互不覆盖 |
| 条件组合（coord） | 一个 `{维度key: 取值}` 的字典，如 `{}`（不写条件=默认）、`{"地区": "华东"}`。条件组合相同的答案构成一条**版本链** |
| 版本链 | 同一知识点下、同一 `coord` 的答案按写入顺序排列形成的链；再编辑 = 在链尾追加新版本，历史版本永久只读保留，**没有物理删除** |
| 默认答案 | `coord = {}` 的那条版本链；查询未带任何条件时优先返回它 |
| 撤回 | 对某个条件组合的**整条版本链**做逻辑删除（不是删单个版本），必须填写原因；撤回后该条件下查不到结果，但历史与留痕永久可查。**撤回不是终态**：往该链写入新版本（新建/编辑/迁移进来）会把整条链恢复为未撤回状态 |
| 解析（resolve） | 给定"知识库+知识点+查询时间+条件"，按 §6 的规则算出应该返回哪条答案，是否精确命中还是权重回退命中 |

---

## 2. 调用约定

- 无统一路径前缀，例如知识库接口就是 `POST /knowledge-bases`，不是 `/api/v1/knowledge-bases`；
- 所有响应统一包一层：

```json
{ "code": 200, "data": { }, "msg": "操作成功" }
```

  - `code` 只有两个值：**`200` 成功，`444` 失败**——与 HTTP 状态码是两套独立的信号，即使 HTTP 是
    400/404/422/500，body 的 `code` 也固定是 444；HTTP 状态码本身仍然带着真实语义，可以用来做
    程序化分支（400=参数/业务错误，404=资源不存在，422=请求体/查询参数格式不对，500=内部错误）；
  - 失败时 `data` 固定是 `{}`，具体原因**只能看 `msg` 文本**——v1 阶段没有做细分错误码（枚举型
    `error_code` 字段），见 §8；
  - 成功的列表接口 `data` 直接是数组，不额外包一层分页结构（**没有分页**，见 §9）。
- 除 422（请求体/查询参数本身不合法，如少了必填字段、类型不对）会返回统一的
  `"请求参数校验失败"`（字段级细节只记服务端日志，不回传给调用方）之外，业务规则相关的错误
  （命名重复、状态不允许、条件不合法等）都是 400，`msg` 是可读的中文原因，直接展示/记录即可；
- 写操作（`POST`/`PATCH`/`PUT`）的 HTTP 成功状态码统一是 **200**，不是 201——即便是"创建"接口。

---

## 3. 维度接口：第三方要先知道能查什么条件

### 3.1 查询维度定义列表

```
GET /dimensions
```

只返回全局状态为 `active` 的维度；已停用的不出现在这里（内部管理另有 `/admin/dimensions` 能看到
全部，含已停用与统计，非对接方需要）。

响应 `data`：

```json
[
  { "key": "地区", "label": "地区", "field_type": "text", "weight": 60 }
]
```

`key` 本身就是展示名（创建时"填 label 同时作为 key"，创建后 `key`/`field_type` 都不可改，
只有 `label`（其实就是改名）、`weight`、`default_value` 可以后续调整——改名不会改 `key`，
沿用旧 `key` 依然能用）。

### 3.2 查询某知识库启用了哪些维度

```
GET /knowledge-bases/{kb_id}/enabled-dimensions
```

第三方查询某个知识点前，**用这个接口确认这个知识库能用哪些维度做条件**——`coord` 里传了
未启用的 key 会被拒绝（见 §6）。返回结构与 3.1 相同，同样只含全局 `active` 的维度。

---

## 4. 知识库接口

| 接口 | 说明 |
|---|---|
| `POST /knowledge-bases` | 创建，`{name, description?}`。`name` 全局唯一（大小写/重音不敏感，`"FAQ"` 与 `"faq"` 视为同一个名字），重复返回 400 `知识库名称已存在，请使用其他名称` |
| `GET /knowledge-bases?status=active\|deprecated` | 列表，不传 `status` 返回全部 |
| `PATCH /knowledge-bases/{kb_id}` | 更新，`{name?, description?}`；`description` 显式传 `null` 会清空，不传则保留原值 |
| `POST /knowledge-bases/{kb_id}/activate` / `.../deactivate` | 启停用，幂等 |
| `PUT /knowledge-bases/{kb_id}/enabled-dimensions` | 整体替换启用维度集合，`{"dimension_keys": ["地区", "客户等级"]}`。传 `[]` = 清空全部；传了不存在或已停用的 key 直接 400 |
| `GET /knowledge-bases/{kb_id}/stats` | 统计卡：`{subject_count, active_answer_count, enabled_dimension_count, today_change_count}`，均只统计当前 `active` 的知识点范围内的数据 |

单条对象响应形状：

```json
{
  "id": 1,
  "name": "示例知识库",
  "description": null,
  "status": "active",
  "active_knowledge_point_count": 12,
  "created_at": "2026-08-01T10:00:00",
  "updated_at": "2026-08-05T09:00:00"
}
```

知识库 `deprecated` 只是个状态标记，**不会连带隐藏其下知识点**——查询/列表接口不检查所属知识库
状态，只按 `kb_id` 是否存在校验（404 `知识库不存在`）。

---

## 5. 知识点与答案：写入侧

均挂在 `/knowledge-bases/{kb_id}/knowledge-points` 前缀下。

### 5.1 创建知识点

```
POST /knowledge-bases/{kb_id}/knowledge-points
```

```json
{
  "title": "退货政策",
  "default_answer": { "content": "支持7天无理由退货", "effective_time": "2026-01-01", "note": "" }
}
```

`default_answer` 可省略（先建空知识点，答案后补）；给了就相当于顺带创建一条 `coord={}` 的默认
答案。`title` 在同一知识库内唯一，重复 400 `知识点标题已存在，请使用其他标题`。

### 5.2 批量导入

```
POST /knowledge-bases/{kb_id}/knowledge-points/batch-import
```

```json
{ "items": [ { "title": "A" }, { "title": "B", "default_answer": {...} } ] }
```

**单批最多 500 条**，用于迁移存量数据。**逐条独立提交，部分成功**——某一条标题冲突不会影响其他
条目：

```json
{
  "created_count": 1,
  "failed_count": 1,
  "results": [
    { "index": 0, "status": "created", "title": "A", "knowledge_point_id": 101 },
    { "index": 1, "status": "failed", "title": "B", "reason": "知识点标题已存在，请使用其他标题" }
  ]
}
```

只有"标题重复"这类约束冲突会被计入某一条的失败原因；如果批次中途发生数据库连接类的严重错误，
整个请求会直接报 500，此前已处理的条目**不会**被当成成功持久化（同一个事务，整体回滚）。

### 5.3 更新 / 软删除 / 恢复

| 接口 | 说明 |
|---|---|
| `PATCH /.../{kp_id}` | 改标题，`{"title": "新标题"}`。**知识点已软删除时禁止**，返回 400 `知识点已删除，无法编辑标题` |
| `POST /.../{kp_id}/delete` | 软删除，`{"delete_reason": "..."}` 必填。幂等——重复调用不会覆盖第一次记录的原因 |
| `POST /.../{kp_id}/restore` | 恢复，幂等，`deleted_at`/`delete_reason` 不会被清空（作为历史保留） |

软删除是**知识点级别的粗粒度操作**，独立于下面"答案撤回"这种条件组合级别的细粒度操作，两者互不
影响。已删除的知识点：查询/条件查询接口把它当成"没有答案"（`resolve` 返回 `status: "none"`），
但历史答案、变更留痕仍然可以正常读取（详情页用，不影响审计）。

### 5.4 写答案

```
POST /.../{kp_id}/answers
```

```json
{
  "content": "支持7天无理由退货，仅限未拆封商品",
  "effective_time": "2026-08-01",
  "coord": { "地区": "华东" },
  "note": "8月起收紧条件"
}
```

`coord` 省略则默认为 `{}`（写入默认答案链）。`coord` 里出现本知识库**未启用**的维度 key，或值类型
与该维度 `field_type` 不匹配，一律 400 拒绝，不做静默忽略（见 §9 的类型校验细节）。

**2026-08-10 起：撤回不再是终态。** 如果这个 `coord` 对应的版本链之前被撤回过，直接写入新版本会
成功，并把该链**全部历史版本**一并恢复为未撤回状态（不是只让新写入的这一行未撤回，避免同一条链
一部分撤回、一部分不撤回的矛盾状态）。

同一 `coord` 下允许多条 `effective_time` 完全相同的答案；此时以 `created_at` 更晚的一条作为该组
当前生效的版本（同一天内多次编辑，最后一次操作胜出）。

### 5.5 编辑答案 —— 追加版本，或迁移条件

```
POST /.../{kp_id}/answers/{answer_id}/edit
```

```json
{ "content": "新内容", "effective_time": "2026-09-01", "note": "" }
```

不传 `coord`：在 `answer_id` 所在的版本链**尾部追加新版本**，历史版本永久只读保留，无需额外原因。

传了不同的 `coord`（即"迁移条件"）：

```json
{
  "content": "新内容",
  "effective_time": "2026-09-01",
  "coord": { "地区": "华南" },
  "migration_reason": "原条件写错了，应该是华南"
}
```

- 原条件组合的**整条版本链会被整体撤回**（`revoked=true`，记录 `migration_reason` 作为撤回原因），
  在新条件组合下新建一条版本链的首个版本——除非原链自己在这次编辑之前就已经是撤回状态，那种情况
  下这条 UPDATE 不会覆盖它原本的撤回原因/时间/操作人；
- `migration_reason` 此时**必填**，缺了 400 `变更适用条件需要填写迁移原因`；
- 若目标条件组合本身是一条被撤回的链，迁移会成功，并把目标链恢复为未撤回状态（同 §5.4 的
  "撤回不是终态"）；
- `coord` 显式传 `null`（JSON `null`，不是省略）会被拒绝——省略表示"条件不变"，传 `{}` 表示
  "改为默认条件"，两者含义不同，`null` 两者都不是，直接 422/400 拒绝，避免误解。

编辑一条已撤回的答案（`revoked=true`）现在也是允许的：不改条件就在原链尾部追加新版本并恢复整条
链为未撤回；改条件（迁移）就按上面的规则处理目标链。

### 5.6 设为默认 / 撤回

| 接口 | 说明 |
|---|---|
| `POST /.../{kp_id}/answers/{answer_id}/promote-to-default` | 把某条答案的**内容**复制成默认答案链（`coord={}`）的一条新版本；源答案本身不受影响。目标（默认链）若已被撤回，会成功并恢复该链为未撤回状态 |
| `POST /.../{kp_id}/answers/{answer_id}/revoke` | 撤回 `answer_id` 所在的**整条版本链**，`{"revoke_reason": "..."}` 必填。并发重复调用是安全的——"第一次成功撤回时记录的原因"生效，不会被后续重复请求覆盖 |

撤回不检查知识点是否已软删除——两者是独立维度，可以分别操作。

---

## 6. 条件查询：对接方的核心读接口

### 6.1 单点解析（推荐第三方直接用这个）

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve?at=2026-08-10&coord={"地区":"华东"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `at` | 今天 | 查询时间点，`YYYY-MM-DD` |
| `coord` | `{}` | JSON 对象编码后的查询字符串，如 `?coord={"地区":"华东","客户等级":"VIP"}` |

响应：

```json
{ "status": "exact", "answer": { "id": 55, "coord": {"地区": "华东"}, "content": "...", "effective_time": "2026-08-01", "revoked": false, ... } }
```

已知这是哪个知识点、想直接问"当前条件下答案是什么"时用这个接口，**不需要先拉列表再自己筛选**。
该知识点不存在返回 404 `知识点不存在`；已被软删除则返回 `200` + `{"status": "none", "answer": null}`
（视为"没有答案"，不是错误）。

### 6.2 列表 + 条件筛选

```
GET /knowledge-bases/{kb_id}/knowledge-points?status=active&keyword=退货&at=2026-08-10&coord={"地区":"华东"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `status` | `active` | `active` / `deleted` |
| `keyword` | 无 | 标题模糊匹配，大小写不敏感，最长 255 字符 |
| `at` | 今天 | 同 6.1 |
| `coord` | 无 | 同 6.1；**不传时不按解析结果过滤**——展示知识库内全部匹配关键字的知识点，每条预览各自的
  `resolved` 字段（可能是默认答案，也可能是权重回退命中的答案）。**一旦传了非空 `coord`，只返回
  "存在与条件兼容的答案"的知识点**——`resolved.status` 为 `none` 的会被整条排除，不会出现在列表里 |

列表每一项在 `KnowledgePointOut` 基础上多带一个 `resolved` 字段：

```json
{
  "id": 12, "title": "退货政策", "status": "active", "active_answer_count": 3, ...,
  "resolved": { "status": "weighted", "answer": { "...": "..." } }
}
```

### 6.3 解析算法（`status` 字段怎么来的）

对每个知识点，在给定的 `at` + 查询条件 `Q` 下：

1. 只考虑 `effective_time <= at` 且未撤回的答案版本；同一 `coord` 内取该组"当前版本"——按
   `(effective_time, created_at, id)` 三元组取最大的一条（同一天内后写的胜出，`id` 只是最终兜底，
   排除数据库返回顺序不确定带来的抖动）；
2. **`Q` 为空**：若存在 `coord={}` 的默认答案组 → `status: "default"`；否则退化为"取全部答案组里
   `(effective_time, created_at, id)` 最大的一条" → `status: "fallback-latest"`；
3. **`Q` 非空**：筛出"条件兼容"的答案组——该组自己写明的每个维度取值都必须与 `Q` 里对应维度一致，
   `Q` 问到但该组没写的维度不参与比较，该组写了但 `Q` 没问到的维度也不参与比较（`coord={}` 的组
   因为什么都没写，永远兼容，充当权重最低的候选）；
   - 无兼容候选 → `status: "none"`，`answer: null`；
   - 有候选：按 `(涉及维度数 spec, 涉及维度权重总和 weight, effective_time, created_at, id)` 取
     最大的一条；
   - 若该结果的条件组合与 `Q` **完全一致**（维度个数、取值都对上）→ `status: "exact"`；
     否则 → `status: "weighted"`（按权重回退命中，不是精确命中，务必在展示时区分）。

条件之间固定是"且"关系，**不支持"或"**；数值/日期维度**只做精确相等匹配，不支持区间或
大于/小于**（`>=`/`<=`/`between` 之类的查询语义目前不存在）。

### 6.4 只读的答案分组树

```
GET /.../{kp_id}/answer-groups?at=2026-08-10
```

返回该知识点**全部**条件组合分组（含已撤回的，标 `revoked: true`），每组带 `version_count` 与
`latest_answer`/`live_answer`（`live_answer` 若该组在 `at` 时点还没有生效版本，为 `null`）。
用于展示"这个知识点历史上到底配置过哪些条件"，不做筛选，不参与解析。

---

## 7. 变更留痕与审计日志

| 接口 | 说明 |
|---|---|
| `GET /.../{kp_id}/change-log` | 单个知识点的写操作时间线：创建/编辑/撤回，每条带 `before_content`/`after_content` |
| `GET /change-log` | **全局**日志，跨所有知识库，每条额外带 `knowledge_base_id`/`knowledge_base_name`/`knowledge_point_title`，方便不预先拉知识点标题就能展示 |

两者都是**只读审计视图，不受知识点软删除 / 知识库停用影响**——历史永久可查，与 §6 的查询接口刻意
不同（查询接口只关心"现在有效的答案"，日志关心"发生过什么"）。`status` 字段含义：

| status | 含义 |
|---|---|
| `live` | 该版本链当前生效的最新版本 |
| `superseded` | 已被同一条件组合下更晚的版本取代 |
| `revoked` | 该版本链已被撤回（撤回动作本身也会生成一条 `action: "revoke"` 的合成记录） |

`revocable` 字段告诉你这一行**现在**能不能调撤回接口（只有"未撤回的最新版本"是 `true`）。

**没有分页**——`GET /change-log` 是全量返回，数据量大时请自行截断/缓存，这是产品文档里明确接受的
已知限制，不是遗漏。

---

## 8. 错误处理

HTTP 状态码保留真实语义；body 的 `code` 恒定 200/444，具体原因看 `msg`。**v1 没有做枚举型
`error_code`**，如果你方需要按错误类型做程序化分支，请先按 `msg` 文本匹配（未来若加错误码会另行
通知，不会不打招呼改文案）。已知会出现的 `msg`（非穷举，新增校验可能带来新文案）：

| HTTP | `msg` | 触发场景 |
|---|---|---|
| 400 | `知识库名称已存在，请使用其他名称` | 创建/改名知识库撞名（大小写/重音不敏感） |
| 400 | `维度「X」不存在` / `维度「X」已停用，无法启用` | 设置知识库启用维度时传了非法 key |
| 400 | `启用维度失败，请刷新后重试` | 设置启用维度时的并发冲突（维度状态在校验后被改变） |
| 400 | `知识点标题已存在，请使用其他标题` | 创建/改名知识点撞名（同知识库内） |
| 400 | `知识点已删除，无法编辑标题` / `...无法写入答案` / `...无法编辑答案` | 对软删除的知识点做写操作 |
| 400 | `变更适用条件需要填写迁移原因` | 编辑答案迁移条件但没填 `migration_reason` |
| 400 | `维度 X 未在本知识库启用` | `coord` 用了未启用的维度 key |
| 400 | `维度 X 取值类型错误，应为...` / `...精度超出...范围` | `coord` 取值类型/精度不满足该维度 `field_type`（详见 §9） |
| 400 | `coord 参数必须是一个 JSON 对象` | 查询用的 `coord` 查询串解析后不是对象（如传了数组） |
| 404 | `知识库不存在` / `知识点不存在` / `答案不存在` / `维度不存在` | 路径里的 ID/key 找不到对应资源 |
| 422 | `请求参数校验失败` | 请求体/查询参数本身格式不对（缺必填字段、类型不对、`coord` 查询串不是合法 JSON）。字段级细节只在服务端日志里，不回传 |
| 500 | `内部错误` / `database unavailable` | 服务端异常或数据库不可用，可重试 |

---

## 9. 字段类型与限制

### 9.1 `coord` 取值的类型校验（写入与查询共用同一套规则）

`coord` 里每个 key 必须是该知识库已启用的维度，取值按该维度的 `field_type` 校验：

| `field_type` | 允许的取值 | 说明 |
|---|---|---|
| `text` | 字符串 | 会 `.strip()`；**去空格后为空串等价于"没写这个条件"**（不会存成空字符串，会被整条丢弃） |
| `number` | JSON 数字或数字字符串 | 整数安全范围 `[-2^63, 2^64-1]`；超出 `2^53` 的整数建议以**字符串**传入以保留精度，浮点数超过约 15~17 位有效数字的精度会被拒绝（`精度超出支持范围`），避免两个"看起来不同"的值被静默当成同一个 |
| `date` | `"YYYY-MM-DD"` 字符串 | 必须是合法 ISO 日期，不接受带时间的字符串 |
| `boolean` | JSON `true`/`false` | **严格类型**，字符串 `"true"`/`"false"` 会被拒绝 |

不合法的取值一律 400，`msg` 里带具体维度 key 和原因，不做自动纠正或静默丢弃。

### 9.2 长度限制一览

| 字段 | 限制 |
|---|---|
| 知识库 `name` | 1~255 字符，去空格后非空，全局唯一（大小写/重音不敏感） |
| 知识库 `description` | ≤ 2000 字符 |
| 维度 `label`（创建时同时作为 `key`） | 1~100 字符，**不能包含 `/`**（会被当成路径分隔符，创建后就无法通过 PATCH/activate 等接口定位） |
| 维度 `label`（更新时） | 1~255 字符（不受创建时 100 字符的限制，但已经存在的 `key` 不受影响） |
| 维度 `weight` | 整数 1~100 |
| 维度 `default_value` | ≤ 255 字符 |
| 知识点 `title` | 1~255 字符，去空格后非空 |
| 知识点 `delete_reason` | 1~500 字符 |
| 批量导入 `items` | 1~500 条/批 |
| 答案 `content` / `note` | 至少 1 字符（`content`），无上限（`LONGTEXT`），`note` 可为空 |
| 答案 `revoke_reason` / `migration_reason` | 1~500 字符 |
| 列表接口 `keyword` | ≤ 255 字符 |
| `PUT enabled-dimensions` 的 `dimension_keys` | 最多 200 个，每个 ≤ 100 字符 |

### 9.3 ID 与时间

- 所有主键（知识库/知识点/答案 ID）由系统生成的**自增整数**，序列化为 JSON 数字（不是字符串）；
- 时间字段（`created_at`/`updated_at`/`revoked_at`）是不带时区信息的 `datetime` 字符串
  （ISO 8601，无 `Z`/偏移量）——服务端假定应用与数据库同一时区，未做显式时区转换；
- `effective_time`/`at`/日期型维度取值统一是纯日期字符串 `"YYYY-MM-DD"`，不含时间部分。

---

## 10. 已知限制（对接前务必确认是否可接受）

- **不支持第三方自带外部 ID 做 create-or-update（幂等 upsert）**。知识点、答案的主键都是系统生成
  的自增整数；第三方需要**自行维护**"系统 ID ↔ 自己的业务 ID"的映射关系，重复调用创建接口会创建
  出新记录（知识点标题唯一约束是唯一的保护，答案没有任何天然去重）；
- **没有分页**。列表类接口（知识点列表、维度列表、答案分组、全局变更日志）都是一次性全量返回，
  数据量评估与分页方案留待后续；
- **不支持跨知识库的批量/增量检索**。所有查询/列表接口都限定在单个 `kb_id` 范围内；
- **并发写冲突不做特殊处理**。同一知识点、同一条件组合被并发编辑/撤回时没有加锁，最后写入的生效
  （数据库唯一约束/业务前置校验能兜住的场景例外，如启用维度的并发竞争会转成一个可重试的 400）；
- **没有角色权限区分**。所有能访问该接口的调用方拥有同等的增删改查能力，`operator` 目前固定是
  `"admin"`。

---

## 11. 常见问题

**能不能只查"某个条件下有没有答案"，不关心具体是精确命中还是权重回退？**
可以，看 `resolve` 响应的 `status` 是不是 `none`——`none` 就是没有；其余四种状态都代表"有"，只是
命中方式不同。

**知识点被软删除后，历史答案是不是也一起没了？**
不会。软删除只影响"查询/列表接口是否能查到它"（`resolve` 视为 `status: "none"`），历史答案与
变更留痕（`/change-log`、`/answer-groups`）永久可查，恢复（`restore`）后立刻恢复可查询状态。

**同一个知识点下，能不能同时存在"华东"和"华南"两条互不影响的答案？**
可以，这正是这个系统的设计目的——不同 `coord` 的答案彼此独立并存，编辑一条不影响另一条。

**维度的 `weight` 具体怎么影响结果？**
只在"没有精确命中、需要按权重回退"时起作用：候选答案先比"写明的维度个数"（更具体的优先），
个数相同再比"涉及维度的权重总和"（权重更高的优先），还相同再比生效时间/创建时间（更新的优先）。
