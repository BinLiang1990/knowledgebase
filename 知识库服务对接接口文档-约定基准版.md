# 知识库服务 · 对接接口文档（经验库对接 · 约定基准版）

| 项目 | 内容 |
|---|---|
| 版本 | 约定基准版（2026-08-10；2026-08-14 增补统一身份认证上线说明——**§5 只读对接面不受影响、对接方零适配**，详见 §1） |
| 地位 | 双方对接约定签署链第 5 项——**技术基准**。与《疑问确认单 v2》《解决版 v2.1》《答复追问函（答复版）》《回执》及《录入规范》（待会签）共同构成完整对接约定 |
| 替代关系 | 替代 v1 初版及 2026-08-10 通用更新版，作为经验库对接的唯一技术基准；如与服务端代码 / Swagger 冲突，以代码为准并提 issue 更新本文档 |
| 适用范围 | 企业打标 / AI 小助手打标的**纯读对接**：对接方只读取，不写入；数据维护全部在知识库服务侧 |
| 依据 | 服务端代码（`backend/src/kb_backend`）逐条核对 + 签署链各文档已确认条款 |

---

## 0. 对接面声明：只读

按双方确认（解决版 C6），**对接面收窄为只读**：

- 对接方使用的接口仅限本文档 §5 列出的 GET 接口；
- 写接口（知识点/答案的增删改，见附录 A）由知识库服务方运营侧使用，对接方不调用；**服务方对写接口的后续变更不另行通知对接方**（写入约定豁免条款）；
- 读接口的字段与行为变更仍受本文档的通知承诺约束（§7、§11）。

数据进入系统只有两条途径，均不经对接方系统：存量一次性交接（解决版 B4）与日常运营录入（§12）。

---

## 1. 部署与安全前提

> **2026-08-14 更新：知识库服务已接入统一身份认证平台（SSO）**，取代原"v1 全接口无鉴权"的阶段性状态。对对接方的影响归零化处理如下。

**分面鉴权**：

- **§5 只读对接面（本文档全部对接接口）：维持无鉴权**——无 API Key / 无签名 / 无 Token / 无限流，服务器间直接调用，与 v1 完全一致。这是对"后续鉴权保留兼容过渡期，不一刀切"承诺的兑现：对接方**零适配**。
- **对接面之外的全部接口**（写接口 + 运营侧读接口）：需携带统一身份认证 `IDENTITYTOKEN`，未携带返回 HTTP 401（`{"code":444,"msg":"登录已过期，请重新登录"}`）。对接方按 §0 约定本就不调用这些接口。

**随之而来的字段行为变化（受 §7 通知承诺约束，本次更新即书面通知）**：

- 写操作 `operator` 不再固定 `"admin"`——自统一身份认证上线起，新写入的答案版本、变更留痕（§5.5 change-log）的 `operator` 为统一平台认证的**操作人真实姓名**。历史数据中的 `"admin"` 保持原样。字段类型与结构不变，对接方无需适配，仅值域变化。

**对接面的后续鉴权规划**（预告，无排期承诺）：统一身份管理系统提供"服务间机器凭证"能力，§5 对接面未来切换到机器凭证时仍按既有承诺执行"发凭证 → 双方联调 → 约定日期启用"的过渡流程，不一刀切。企业库 HMAC 方案不可复用的结论不变。

**上线前置条件**（解决版 A1 结论，挂遗留事项 #1）：部署于内网/专线，或网关层 IP 白名单就位。网络边界、白名单申请流程、部署时区（预期 UTC+8）由双方运维在上线检查单中书面固化。CORS 只约束浏览器跨域，对服务器间调用无限制（不变）。

---

## 2. 核心概念

| 概念 | 说明 |
|---|---|
| 知识库 | 顶层容器，`status` 为 `active`/`deprecated`。知识点、答案归属唯一知识库；不支持跨库检索 |
| 维度 | 全局定义的条件字段。`key`（即展示名，创建后不可改）、`field_type`（`text`/`number`/`date`/`boolean`）、`weight`（1~100，权重回退用）、`status` |
| 知识库启用维度 | 每库从全局维度勾选子集；只有本库启用且全局 `active` 的维度可出现在答案条件、查询条件中 |
| 知识点 | 可被问到的"问题"。同库内 `title` 唯一；`status` 为 `active`/`deleted`（软删除，可 restore） |
| 答案 | 知识点在某条件组合下的具体说法：`content`、`effective_time`、`coord`、`note`、`revoked`。不同 `coord` 的答案彼此独立并存 |
| 条件组合（coord） | `{维度key: 取值}` 字典；`{}` 即默认答案。相同 coord 的答案构成一条**版本链** |
| 版本链 | 再编辑 = 链尾追加新版本，历史版本永久只读保留，**无物理删除** |
| 撤回 | 对整条版本链的逻辑删除，原因必填。**撤回不是终态**（2026-08-10 起）：向该链写入新版本会恢复整条链，详见 §9 |
| 解析（resolve） | 给定"知识库 + 知识点 + 查询时间 + 条件"，按 §6 规则算出返回哪条答案 |

---

## 3. 建模约定（已确认，解决版 B1）

- **三个知识库分开建**：`tagging`（打标经验）/ `tagging_skill`（小助手播报规则）/ `operate_skill`(小助手操作规则) 各一库。不采用"单库 + library 维度"（coord 为兼容匹配，library 条件漏写即串库，且无分页下单库数据量更大）；
- 租户隔离用 `tenant` 维度（text 类型）表达：单租户经验 `coord={"tenant": "..."}`，全局经验 `coord={}`；
- **全局经验白名单**：哪些知识点允许 `coord={}` 由双方在存量交接时共同确认后冻结，新增全局经验需双方知会（回执三.2 动作 #5）；
- "全局经验"在三库间的重复维护成本由服务方录入侧承担；
- 对接方**所有读取强制携带非空 `tenant` coord**（对接方内部约束，同时是 §6 fallback-latest 风险的完全规避手段）。

---

## 4. 调用约定

- 无统一路径前缀（`GET /knowledge-bases`，非 `/api/v1/...`）；
- 所有响应统一包一层：

```json
{ "code": 200, "data": { }, "msg": "操作成功" }
```

  - `code` 只有 `200`（成功）/ `444`（失败）两个值，与 HTTP 状态码是两套独立信号；
  - **程序化分支只依赖 HTTP 状态码**（400=参数/业务错误，404=资源不存在，422=请求格式错误，500=内部错误），`msg` 仅透传展示——双方已确认此策略（解决版 C4）；
  - v1 无枚举 `error_code`；**错误文案变更提前书面通知，不会不打招呼改文案**（已写入对接约定）；
  - 成功的列表接口 `data` 直接是数组，**没有分页**（§13）。

**超时与重试**（解决版 C5，联调压测后修订终值）：

| 项 | 约定 |
|---|---|
| 可安全重试 | 全部 GET 接口均为纯读、无副作用，可任意重试；500 `database unavailable` 类错误可重试 |
| 客户端超时初值 | `resolve` 3 秒；全量列表 / change-log 10 秒 |
| 并发上限 | 待测试环境联调压测后书面确定（遗留事项 #2）；服务端当前无限流 |
| 降级 | 对接方保留"经验库异常 → 无经验打标"降级兜底 |

---

## 5. 对接方读接口（对接面全集）

### 5.1 维度

```
GET /dimensions
GET /knowledge-bases/{kb_id}/enabled-dimensions
```

均只返回全局 `active` 的维度：`[{ "key": "tenant", "label": "tenant", "field_type": "text", "weight": 60 }]`。查询前用后者确认该库可用哪些维度做条件——`coord` 传未启用的 key 会被 400 拒绝。

### 5.2 单点解析（resolve）

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve?at=2026-08-10&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `at` | 今天（服务端时区） | 查询时间点，`YYYY-MM-DD` |
| `coord` | `{}` | JSON 对象编码进查询串 |

响应 `data`：`{ "status": "exact" | "weighted" | "default" | "fallback-latest" | "none", "answer": {...} | null }`。

- 知识点不存在 → 404 `知识点不存在`；已软删除 → `200` + `{"status": "none", "answer": null}`（视为无答案，不是错误）；
- `status` 为 `none` 以外的任何值都表示"有答案"，区别只是命中方式（§6）。

### 5.3 列表 + 条件筛选（打标全量拉取用）

```
GET /knowledge-bases/{kb_id}/knowledge-points?status=active&keyword=退货&at=2026-08-10&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `status` | `active` | `active` / `deleted` |
| `keyword` | 无 | **仅标题**模糊匹配（大小写不敏感，≤255 字符）；不支持内容/备注搜索，无排期（解决版 C1，对接方已接受） |
| `at` / `coord` | 同 5.2 | 传了非空 `coord` 后，`resolved.status` 为 `none` 的知识点整条排除，不出现在列表里 |

每项在知识点字段基础上带 `resolved` 字段（结构同 5.2 响应）。

> **全文承诺（解决版 A2 阻塞解除的依据）**：`resolved.answer` 与 resolve 使用同一响应模型（`AnswerOut`），`content` 为必选字段、返回**全文**、无摘要截断形态。按租户全量拉取**一次列表调用即可拿全部生效经验含全文，不存在 N+1**。

### 5.4 答案分组树（审计 / 巡检用）

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answer-groups?at=2026-08-10
```

返回该知识点**全部**条件组合分组（**含已撤回**，标 `revoked: true`），每组带 `version_count`、`latest_answer`、`live_answer`（`at` 时点无生效版本则为 `null`）。不做筛选、不参与解析。tenant 巡检脚本（§12.3）即基于此接口。

### 5.5 变更留痕

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/change-log   （单知识点）
GET /change-log                                                    （全局，跨库）
```

写操作时间线（创建/编辑/撤回），每条带 `before_content`/`after_content`；全局版每条额外带 `knowledge_base_id`/`knowledge_base_name`/`knowledge_point_title`。只读审计视图，不受软删除/停用影响。

- `status`：`live`（链当前生效版本）/ `superseded`（被更晚版本取代）/ `revoked`（链已撤回，撤回动作另有 `action: "revoke"` 合成记录）；
- **无分页，全量返回**。对接方快照核对**优先用单知识点接口定点核对**，全局接口仅审计用并自行截断（解决版 A2）；
- ⚠️ 留痕为答案行的**派生视图**而非独立事件表，"已恢复链"的历史撤回不可见——已知缺陷，详见 §9.3。

### 5.6 统计（交接对账用）

```
GET /knowledge-bases/{kb_id}/stats
```

`{subject_count, active_answer_count, enabled_dimension_count, today_change_count}`，只统计 `active` 知识点范围。存量交接对账：stats 条数核对 + 按租户抽样 resolve 比对全文（解决版 B4）。

---

## 6. 解析算法

对每个知识点，在给定 `at` + 查询条件 `Q` 下：

1. 只考虑 `effective_time <= at` 且未撤回的答案版本；同一 `coord` 内按 `(effective_time, created_at, id)` 取最大者为该组当前版本（同日多次编辑，最后一次胜出）；
2. **`Q` 为空**：有 `coord={}` 默认组 → `default`；否则取全部组里最新的一条 → `fallback-latest`；
3. **`Q` 非空**：筛"条件兼容"的组——该组写明的每个维度取值必须与 `Q` 一致；`Q` 问到但组没写的维度不参与比较（**因此 `coord={}` 组永远兼容**，充当最低优先级候选）：
   - 无候选 → `none`；
   - 有候选：优先取"自身条件被 `Q` 完全覆盖"的组，再按 `(涉及维度数, 维度权重和, effective_time, created_at, id)` 取最大；
   - 结果条件与 `Q` 完全一致 → `exact`；否则 → `weighted`（权重回退命中，展示时务必区分）。

条件间固定"且"，**不支持"或"**；数值/日期维度只做精确相等，不支持区间比较。

**两条已确认的推论：**

- **fallback-latest 不会出现在对接方链路上**——它仅在 `Q` 为空且无默认答案时出现，对接方强制带非空 `tenant` coord（§3）后该状态不可能出现；非空 `Q` 无候选时返回 `none`，不会兜底乱返（解决版 C2，风险闭环）；
- **未写 `tenant` 的答案对所有租户查询都是兼容候选**——服务无"维度必填"能力且短期无排期，该风险由录入规范 + 定期巡检兜底（§12.3，解决版 A3）。

---

## 7. 答案对象与字段承诺

resolve、列表 `resolved`、分组树中的答案对象为同一结构（`AnswerOut`），固定携带：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | int | 答案版本 ID。编辑=追加新版本，`id` 随之变化；历史版本行永久只读保留 |
| `knowledge_base_id` / `knowledge_point_id` | int | **必选字段**，非文档示例巧合（解决版 C6 已确认）——对接方快照锚定 `knowledge_point_id` + `coord` |
| `coord` / `coord_hash` | object / string | 条件组合及其规范化哈希 |
| `content` | string | **全文**，无长度上限（LONGTEXT） |
| `effective_time` | date | 生效日期（日期粒度） |
| `note` | string\|null | 版本级备注，编辑追加新版本时不自动继承 |
| `revoked` / `revoked_at` / `revoked_by` / `revoke_reason` | — | 撤回状态与留痕；⚠️ 链恢复时整链清空，见 §9.3 |
| `operator` / `source` / `created_at` | — | 操作与来源留痕（2026-08-14 起 `operator` 为统一身份认证的操作人真实姓名；此前历史数据固定 `admin`，见 §1） |

**承诺**：以上字段只增不删；读接口字段与行为变更提前通知对接方。

---

## 8. kp_id 稳定性承诺（解决版 A4 + 回执追问二）

对接方任务级快照与标签级经验引用以 `kp_id` 为锚点，服务方承诺：

1. **kp_id 永不复用**：知识点只有软删除、无物理删除路径，行永久保留，自增 ID 序列只增不回收；
2. **软删除后 restore，kp_id 不变**（同一行状态翻转，`deleted_at`/`delete_reason` 保留为历史）；
3. 常规运维（数据修复、答案编辑、撤回/恢复）不改变 kp_id；
4. **整库重建/环境迁移的双前置条件**（写入对接约定）：
   - **提前书面通知**对接方；
   - **必附新旧 kp_id 映射表**：JSON（字段 `old_kp_id` / `new_kp_id` / `knowledge_base` / `title` / `unmapped_reason`，可映射性由"库内标题唯一 + 无物理删除"保证；无法映射项单列附原因），需要时可同时出 CSV；**不迟于重建完成后 2 个工作日**随重建完成通知交付；
5. 测试与生产环境 kp_id 必然不同（自增 ID），对接方灰度验证在两套环境分别配置。

---

## 9. 撤回与恢复语义（2026-08-10 行为变更 + 已知缺陷披露）

### 9.1 行为：撤回不是终态

- `POST /.../answers/{answer_id}/revoke` 撤回 `answer_id` 所在**整条版本链**，原因必填；撤回后该条件下 resolve 查不到结果；
- 向被撤回链写入新版本（新建答案、编辑已撤回答案、迁移条件进入该链、promote-to-default 落到已撤回默认链）**均会成功，并把该链全部历史版本一并恢复**为未撤回（保持整链撤回状态一致的不变量，不存在半撤回链）；
- 误撤回可由录入侧写入新版本自助恢复，无单独 "un-revoke" 接口。

### 9.2 风险提示（已写入录入规范，回执 2.3 确认）

> **向已撤回链写入未来生效版本会先复活旧内容。** 链上 `effective_time <= 今天` 的历史旧版本随恢复**立即**重新参与解析，早于新版本生效。若意图是"未来某日起重新启用且不接受旧内容先行生效"，正确做法：新版本 `effective_time` 填当天（按"同日后写胜出"立即成为当前版本），到目标日再追加内容变更；或写入前逐一确认旧版本内容可接受立即复活。

该场景已列入联调用例清单（B5）。

### 9.3 已知缺陷：恢复后撤回留痕消失（回执 2.4，登记在案）

当前实现中，链恢复时：

- **答案行层面**：`revoked_at` / `revoked_by` / `revoke_reason` 整链**清空**，恢复后无法从答案对象读到曾被撤回的痕迹；
- **变更留痕层面**：change-log 为派生视图，撤回合成记录（`action: "revoke"`）仅在链**当前**处于撤回状态时生成——链恢复后该记录消失，时间线呈现为"从未撤回过"；恢复动作本身也无留痕。唯一例外：迁移导致的原链撤回若此后未被恢复，其 `migration_reason` 仍可查。

对接方任务级快照不受影响（快照冻结的是 resolve 结果，缺陷只影响事后追溯撤回过程）。

**双方已确认的处理（回执 2.4 三项条件，为闭环组成部分）：**

| # | 条款 | 状态 |
|---|---|---|
| 1 | 修复（撤回/恢复改为独立事件表）给出**明确版本承诺**，不接受开放式排期 | 目标版本/时间范围：____________【服务方产品回填，随本文档存档一并交付】 |
| 2 | 修复上线前，任何"先撤回、后恢复"发生时，服务方于**恢复当日**通知对接方（kp_id + coord + 撤回时间 + 恢复时间），作为替代记录；仅服务方内部工单留档不满足本条 | 生效中 |
| 3 | 联调用例补充缺陷边界验证：撤回 → 打标（resolve 返回 none，快照记录 degraded/none）→ 恢复 → 核对 change-log 呈现 | 已列入 B5 测试数据清单 |

**兜底条款**：条件 1 承诺版本延期超过一个版本周期，或条件 2 通知义务未履行且导致对接方无法完成打标结果复盘，对接方有权重新评估缺陷等级并要求修复提级。

缓解措施（已写入录入规范）：撤回操作的原因与上下文由运营侧在工单/运营记录中另行留档。

---

## 10. 时间、时区与生效时效

- 时间字段（`created_at` 等）为不带时区的 naive datetime，服务端假定应用与数据库同时区；部署时区预期 **UTC+8**，随上线检查单书面固化（遗留事项 #1）；
- `effective_time` / `at` / 日期型维度取值为纯日期 `"YYYY-MM-DD"`；
- **"当天录入、effective_time 填当天"即时生效**（解析取 `effective_time <= at` 闭区间，`at` 默认今天；同日多版本按 `created_at` 最新胜出），满足紧急修正场景；
- 叠加对接方缓存（打标 60 秒 / 规则库 5 分钟），业务感知的生效延迟为**分钟级**；经验变更**无主动通知**（服务无回调），靠缓存 TTL 自然收敛，业务方已知悉。

---

## 11. 错误表

HTTP 状态码保留真实语义；body `code` 恒 200/444，原因看 `msg`。对接面为只读，实际可能遇到的子集：

| HTTP | `msg` | 触发场景 |
|---|---|---|
| 400 | `维度 X 未在本知识库启用` | 查询 `coord` 用了未启用的维度 key |
| 400 | `维度 X 取值类型错误，应为...` / `...精度超出...范围` | `coord` 取值类型/精度不满足维度 `field_type`（text 会 strip，空串等价未写；number 超精度拒绝；date 须 ISO 日期；boolean 严格 `true`/`false`） |
| 400 | `coord 参数必须是一个 JSON 对象` | `coord` 查询串解析后不是对象 |
| 404 | `知识库不存在` / `知识点不存在` / `答案不存在` / `维度不存在` | 路径 ID/key 找不到资源 |
| 422 | `请求参数校验失败` | 查询参数格式不对（如 `coord` 不是合法 JSON）；字段级细节只记服务端日志 |
| 500 | `内部错误` / `database unavailable` | 服务端异常/数据库不可用，**可重试** |
| 401 | `登录已过期，请重新登录` | 调用了 §5 对接面**之外**的接口且未携带统一身份认证 Token（2026-08-14 起）。§5 对接面接口不会返回此错误；若对接方遇到，说明调用了约定外的接口 |

写接口错误文案见附录 A 所属源文档，对接方无需处理。历史版本文档中的三条撤回相关 400 文案（`该条件组合已被撤回，无法直接写入` / `...无法编辑` / `...无法迁移到该条件`）已随 2026-08-10 行为变更**删除**，正式书面通知已于答复版追问一.2 完成，对接方确认无适配工作（回执 2.2）。

---

## 12. 运营与录入侧约定

### 12.1 账号分工（回执追问四）

- ~~鉴权/身份系统落地前~~（2026-08-14 统一身份认证已上线）：过渡期内**对接方人员仍不直接操作管理端**；全部录入需求（新增/修改/纠错/撤回）统一提交服务方运营执行；
- 响应时效：常规录入需求 **T+1 个工作日**内生效；紧急纠错（影响线上打标质量）**当日响应**。提交渠道（工单/共享表格/邮件）在对接约定签署时确定；
- "写操作 `operator` 记录到具体人员"的前置条件**已满足**（统一身份认证，按操作人真实姓名留痕）。开放对接方账号的节奏与授权范围（本系统角色为 只读/编辑 分档）由双方另行约定，届时责任界定细化为**按操作人归责**。

### 12.2 录入规范要点（对接约定附件，待双方会签）

1. 每条经验**必须写 `tenant` 条件**，除非在全局经验白名单内（§3）；
2. 单条经验内容 ≤ **1500 字**（对接方注入硬限 1600 字截断，超长被截断且不另行通知，快照标记 `truncated` 供事后核对）；单租户生效经验总量对齐对接方 60000 字预算，超出按权重裁剪；
3. 规则库知识点**标题命名规范**（"输出模板"等关键字约定）书面化；**改标题需提前通知对接方**（改标题即时生效且无通知机制）；
4. 撤回链风险提示（§9.2 文本）；撤回原因与上下文在工单/运营记录另行留档（§9.3 缓解措施）。

### 12.3 tenant 巡检（回执追问三）

| 项 | 约定 |
|---|---|
| 频率 | 存量交接完成后**立即首次全量巡检**（与交接对账合并执行）；此后**每周一次** |
| 方式 | 巡检脚本遍历三个知识库全部知识点的 `answer-groups`（§5.4），输出缺失 `tenant` 条件且不在全局经验白名单内的版本链清单 |
| 报告同步 | 每次报告（kp_id + coord + 知识库 + 标题，含"本次新增/上次遗留"标记）经**共享文档**当日同步对接方；清单非空额外**邮件提醒** |
| 错配处置 | 发现后 **1 个工作日内修正**（管理端编辑/迁移条件，即时生效）；修正当日通知对接方受影响的 kp_id + coord + **错配存续时间窗**，供回查该窗口内使用该经验的打标任务 |
| 责任界定 | tenant 漏写/错写导致跨租户注入属录入侧责任；发现机制 = 巡检 + 对接方快照回查 |

---

## 13. 已知限制汇总

| 限制 | 说明 | 对接影响 |
|---|---|---|
| 无分页 | 列表/维度/分组树/change-log 全量返回，无排期 | 对接方按"全量拉取 + 本地缓存"架构设计，已接受 |
| 无增量、无回调 | 无按时间过滤、无变更通知（服务端不主动回调任何地址） | 缓存 TTL 收敛；快照核对优先单知识点 change-log |
| 无跨库检索 | 所有查询限单个 `kb_id` | 三库分建（§3），对接方按库分别拉取 |
| 无幂等 upsert | 主键自增整数，重复创建即新记录 | 对接方只读，不受影响；存量交接由服务方执行并对账 |
| 无维度必填校验 | tenant 条件靠录入纪律 | 录入规范 + 每周巡检兜底（§12.3） |
| 无并发写控制 | 并发编辑最后写入生效 | 写入均在服务方侧，单一录入口径（§12.1）天然收敛 |
| 无 SLA 机制 | 可用性目标、故障/维护通知属运维承诺 | 随遗留事项 #1/#4 一并签署；对接方保留降级兜底 |
| 撤回留痕缺陷 | 已恢复链的历史撤回不可追溯 | §9.3 三项条件约束下登记为已知缺陷，待版本修复 |

---

## 附录 A：写接口简表（非对接面）

以下接口由服务方录入侧与存量交接脚本使用，**对接方不调用**；其变更不另行通知对接方（§0 豁免条款）。详细语义以服务端代码/Swagger 为准。2026-08-14 起写接口需统一身份认证（`IDENTITYTOKEN`，见 §1），存量交接脚本如需运行由服务方提供带认证的执行通道。

| 接口 | 用途 |
|---|---|
| `POST /knowledge-bases` / `PATCH /{kb_id}` / `activate` / `deactivate` / `PUT .../enabled-dimensions` | 知识库管理 |
| `POST /.../knowledge-points` | 创建知识点（可顺带默认答案；title 同库唯一） |
| `POST /.../knowledge-points/batch-import` | 批量导入，单批 ≤500，**仅支持默认答案**；逐条独立提交部分成功（整批单事务，返回前统一提交；严重错误整体 500 回滚）。存量交接中用于全局经验，带 tenant 条件的经验逐条走写答案接口 |
| `PATCH /.../{kp_id}` / `POST .../delete` / `POST .../restore` | 改标题 / 软删除（原因必填，幂等）/ 恢复（幂等，kp_id 不变） |
| `POST /.../{kp_id}/answers` | 写答案（coord 省略即默认链；写入已撤回链会恢复整链，§9） |
| `POST /.../answers/{id}/edit` | 链尾追加新版本；带不同 `coord` 即迁移条件（原链整体撤回，`migration_reason` 必填） |
| `POST /.../answers/{id}/promote-to-default` | 内容复制为默认链新版本 |
| `POST /.../answers/{id}/revoke` | 撤回整条版本链，原因必填，并发安全（首次原因生效） |

## 附录 B：文档演进与签署链

| 版本 | 日期 | 变化 |
|---|---|---|
| v1 初版 | — | 首个对接文档 |
| 2026-08-10 更新版 | 2026-08-10 | 撤回语义变更（不再是终态）；删除三条撤回相关 400 文案 |
| **约定基准版（本文档）** | 2026-08-10 | 按签署链已确认条款重构为纯读对接基准：对接面收窄为只读（§0）、建模约定（§3）、超时/重试约定（§4）、全文承诺（§5.3）、kp_id 稳定性与重建映射表（§8）、撤回留痕缺陷披露与三项条件（§9.3）、运营/录入/巡检约定（§12） |

签署链：① 疑问确认单 v2 → ② 解决版 v2.1 → ③ 追问函及答复版 → ④ 确认回执 → ⑤ **本文档（技术基准）** → ⑥ 录入规范（附件，待会签）。

> 待回填项：§9.3 条件 1 的修复目标版本（服务方产品）；§1 部署边界/时区、§4 并发上限与超时终值（双方运维/联调压测）。
