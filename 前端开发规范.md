# 前端开发规范

> 适用范围：企业服务平台全部前端项目（含后续新建系统）。
> 提炼来源：EnterpriseService.Admin.Web（管理后台）与 EnterpriseService.Board.Web（数据看板）两个已落地项目的现行实践；两者不一致之处已在本文中做出统一决策，以本文为准。
> 版本：v1.0（2026-08）

---

## 0. 总原则

1. **所有系统同一套技术栈、同一套目录骨架、同一套请求层与代码风格**。新项目从模板复制启动，不允许自行更换框架、UI 库、构建工具。
2. **约定优于配置**：本文写明的目录、命名、写法是强约定；本文未覆盖的，参考两个既有项目中一致的做法。
3. **类型即契约**：与后端的字段对齐通过 TypeScript 类型 + 注释（标对接文档章节号）完成，禁止 `any` 满天飞。
4. **注释解释「为什么」**：中文注释，关键取舍必须写明原因，接口/字段注释标注后端对接文档章节号、还原设计稿的样式标注 Figma 节点号。
5. **只装会用的依赖**：装了不用的插件/依赖（历史教训：UnoCSS、pinia-plugin-persistedstate 空转）必须删除。

---

## 1. 技术栈基线

| 类别 | 选型 | 版本基线 |
|---|---|---|
| 框架 | Vue 3（Composition API + `<script setup lang="ts">`） | ^3.5 |
| 语言 | TypeScript（strict 模式） | ^5.6 |
| 构建 | Vite | ^5.4 |
| 路由 | vue-router（hash 模式） | ^4.4 |
| 状态 | Pinia | ^2.2 |
| UI 库 | Element Plus（unplugin 按需自动引入） | ^2.8 |
| 图标 | @element-plus/icons-vue | ^2.3 |
| 请求 | axios + json-bigint | ^1.7 / ^1.0 |
| 图表 | echarts（按需注册） + vue-echarts | ^5.5 / ^7.0 |
| 日期 | dayjs | ^1.11 |
| 工具库 | @vueuse/core | ^11 |
| CSS | SCSS（sass，modern-compiler API） | ^1.79 |
| 代码检查 | ESLint 9（flat config） + @antfu/eslint-config | ^9 / ^3.7 |
| 包管理 | **pnpm**（package.json 写死 `packageManager: pnpm@9.x`） | 9.x |

**明确不用**：prettier / stylelint（风格由 antfu 的 ESLint 统一管，避免规则打架）、tailwind、UnoCSS（历史项目里是死配置，新项目不引入）、CSS Modules、TS `enum`（用字符串字面量联合类型 + `Record` 映射表代替）。

`.npmrc` 统一：

```
shamefully-hoist=true
registry=https://registry.npmmirror.com
```

---

## 2. 工程化配置

### 2.1 package.json scripts（固定五条）

```json
{
  "dev": "vite",
  "build": "vue-tsc --noEmit && vite build",
  "build:pack": "vue-tsc --noEmit && vite build && tar -czf dist.tar.gz dist && node -e \"require('fs').rmSync('dist',{recursive:true})\"",
  "preview": "vite preview",
  "lint": "eslint . --fix",
  "typecheck": "vue-tsc --noEmit"
}
```

- **构建必须先过类型检查**（`vue-tsc --noEmit`），交付物为 `dist.tar.gz`。
- **`dist.tar.gz` 与 `dist/` 一律不提交进仓库**（写入 `.gitignore`）。

### 2.2 ESLint（唯一的风格来源）

```js
// eslint.config.js
import antfu from '@antfu/eslint-config'

export default antfu({
  vue: true,
  typescript: true,
  // 使用 ESLint 自带格式化能力，不引 prettier，避免规则打架
  stylistic: { indent: 2, quotes: 'single', semi: false },
  ignores: ['dist', 'node_modules', 'auto-imports.d.ts', 'components.d.ts', 'public'],
})
```

风格结论：**2 空格缩进、单引号、无分号**；`import type` 置于 import 块最前；提交前必须 `pnpm lint` 通过（历史项目出现过 import 夹在代码中间、导入顺序混乱的情况，均因 lint 未被执行——新项目要求 lint 进入提交流程，见 §13）。

`.editorconfig` 统一：utf-8 / LF / 2 空格 / 末尾换行 / 去尾空格（md 除外）。

### 2.3 tsconfig 关键项（不得放松）

```jsonc
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noEmit": true,
    "isolatedModules": true,
    "skipLibCheck": true,
    "types": ["node", "element-plus/global"],
    "paths": { "@/*": ["src/*"] }
  }
}
```

### 2.4 Vite 关键项

```ts
export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd())
  return {
    // 生产用相对 base，配合 hash 路由，dist 可放任意子目录或本地直开
    base: mode === 'production' ? './' : '/',
    resolve: { alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) } },
    plugins: [
      vue(),
      AutoImport({
        imports: ['vue', 'vue-router', 'pinia', '@vueuse/core'],
        resolvers: [ElementPlusResolver()],
        dts: 'auto-imports.d.ts',
      }),
      Components({ resolvers: [ElementPlusResolver()], dts: 'components.d.ts' }),
    ],
    css: {
      preprocessorOptions: {
        scss: {
          api: 'modern-compiler',
          // 全局变量自动注入，业务文件里不写 @use
          additionalData: `@use "@/styles/variables.scss" as *;`,
        },
      },
    },
    server: {
      host: '0.0.0.0',
      port: 31xx, // 每个系统分配独立端口，见 §2.6
      proxy: {
        [env.VITE_APP_BASE_API]: {
          target: env.VITE_PROXY_TARGET,
          changeOrigin: true,
          rewrite: path => path.replace(new RegExp(`^${env.VITE_APP_BASE_API}`), ''),
        },
      },
    },
  }
})
```

**规范决策（重要）**：

- **`auto-imports.d.ts` / `components.d.ts` 必须提交进仓库**。历史项目将其 gitignore，导致新克隆仓库直接 `pnpm build` / `pnpm typecheck` 因缺声明而大面积报错。
- **统一使用 auto-import**：`ref` / `computed` / `watch` / `onMounted` / `useRouter` / VueUse 等不再手写 import（Board 端现行做法）；`ElMessage` 等 Element Plus API 同理。仅 `.ts` 工具文件中按需显式 import。
- 项目必须有 `public/` 目录并放置 `favicon.ico`（历史项目 index.html 引了不存在的 favicon，必然 404）。

### 2.5 环境变量

- 只用 `.env.development` 与 `.env.production` 两个文件，变量前缀统一 `VITE_APP_*`（代理目标例外：`VITE_PROXY_TARGET`）。
- 每个变量上方写中文注释（用途 + 本地联调如何改）。
- **全部变量必须在 `env.d.ts` 的 `ImportMetaEnv` 中声明**；只在 dev 存在的变量声明为可选（`VITE_PROXY_TARGET?: string`，历史项目声明为必选与事实不符）。

```
# .env.development
VITE_APP_TITLE=xxx系统
# 接口前缀，dev 走 vite 代理并剥掉 /api；生产由 nginx 反代并同样剥前缀
VITE_APP_BASE_API=/api
# 本地起后端时改为 http://localhost:7071
VITE_PROXY_TARGET=https://xxx.example.com
```

### 2.6 端口与应用标识分配

每个系统固定：dev 端口、token 存储 key、`X-App-Type` 值，三者一一对应，禁止复用（Token 不可跨端使用）。

| 系统 | dev 端口 | TOKEN_KEY | X-App-Type |
|---|---|---|---|
| Admin.Web（管理后台） | 3100 | ENTERPRISE_ADMIN_TOKEN | ADMIN |
| Board.Web（数据看板） | 3200 | ENTERPRISE_DASHBOARD_TOKEN | DASHBOARD |
| 后续系统 | 3300 起递增 | ENTERPRISE_<APP>_TOKEN | 大写应用名 |

---

## 3. 目录结构

```
src/
├── main.ts                 # 应用装配入口（创建 app、use 插件、副作用 import guard/echarts）
├── App.vue                 # 只放 el-config-provider(zhCn) + router-view，不写业务
├── settings.ts             # 全局常量：TOKEN_HEADER/TOKEN_KEY/APP_TYPE/业务码/白名单
├── api/                    # 接口层：按后端 Controller 模块一文件（非按页面）
├── components/             # 全局/跨模块复用组件（按需建 common/ dialogs/ widgets/ 等子目录）
├── composables/            # 组合式函数，use 前缀（useTable.ts 等）
├── directives/             # 自定义指令（index.ts 导出 setupDirectives）
├── layout/                 # 布局壳（index.vue + components/）
├── router/
│   ├── index.ts            # 路由实例 + 动态路由挂载/清理
│   ├── constant.ts         # 免鉴权常量路由（login/403/404）
│   ├── asyncRoutes.ts      # import.meta.glob 自动收集 modules
│   ├── guard.ts            # 全局守卫（main.ts 里副作用 import）
│   └── modules/            # 一业务模块一路由文件
├── store/
│   ├── index.ts            # createPinia 装配
│   └── modules/            # 一领域一 store（user.ts / permission.ts / ...）
├── styles/
│   ├── variables.scss      # 只放变量（vite 自动注入），色值取自 Figma 标注
│   ├── index.scss          # 全局基础样式 + 通用工具类
│   ├── element.scss        # Element Plus 主题调优（按组件分节，标 Figma 节点号）
│   └── dialog.scss         # 通用弹窗骨架（.app-dialog）
├── types/                  # 跨模块通用类型（api.ts / router.d.ts / 领域契约）
├── utils/                  # 纯函数工具（request / auth / format / echarts / ...）
└── views/                  # 页面
    └── <module>/
        ├── list/index.vue          # 列表页
        ├── detail/index.vue        # 详情页
        ├── XxxDialog.vue           # 页面私有弹窗，与页面同级
        ├── components/             # 模块内跨页共享组件
        └── shared.ts               # 模块内跨页共享的枚举映射/格式化纯函数
```

规则：

1. **目录一律小写**，页面文件为 `views/<module>/<page>/index.vue`。
2. 组件文件名 **PascalCase**，后缀语义化：`XxxDialog.vue`（弹窗）、`XxxCard.vue`（卡片）、`XxxPane.vue`（弹窗内页）、`XxxShell.vue`（外壳）、`XxxLoader.vue`（装载器）。
3. 复用逻辑的归属：**跨模块 → `src/utils/`（纯函数）或 `src/composables/`（含响应式）；模块内跨页 → `views/<module>/shared.ts`；全局展示组件 → `src/components/`**。禁止跨模块引用另一个模块 views 下的私有文件（历史坏味道：user 模块 `@use '@/views/org/dialog'`）。
4. 单文件组件（SFC）**超过 400 行必须考虑拆分**：表单构建、轮询、导出下载等逻辑抽 composable 或子组件（历史项目有 1100+ 行的巨型弹窗，复用与测试均困难）。

---

## 4. 命名规范

| 对象 | 规则 | 示例 |
|---|---|---|
| 目录 | 小写 | `views/enterprise/list/` |
| 组件文件 | PascalCase + 语义后缀 | `CreateEnterpriseDialog.vue` |
| 模板中自研组件 | PascalCase 标签 | `<CreateEnterpriseDialog />` |
| 模板中 Element Plus | kebab-case 标签 | `<el-table>` |
| 路由 path | **kebab-case**（禁止 camelCase，历史 `/orgUser` 属违规） | `/tag-task` |
| 路由 name | PascalCase，且与页面 `defineOptions({ name })` 严格一致 | `EnterpriseList` |
| store | `useXxxStore`，id 小写 | `useUserStore` / `'user'` |
| composable | `use` 前缀 | `useTable` |
| API 函数 | 动词前缀：`page*` 分页 / `get*`、`fetch*` 查询 / `create*`、`update*`、`delete*` 写操作 | `pageEnterprise` |
| 常量/映射表 | SCREAMING_SNAKE | `SLICE_STATUS_META`、`POLL_INTERVAL` |
| 类型/接口 | PascalCase，无 `I`/`T` 前缀 | `EnterpriseItem`、`PageQuery` |
| 权限码 | `模块:动作` | `enterprise:save` |
| CSS 类 | 语义化 kebab-case + SCSS 嵌套 + 状态类 `.active` / `.is-danger`（不用 BEM） | `.status-cell > .dot` |
| 环境变量 | `VITE_APP_*` | `VITE_APP_BASE_API` |

---

## 5. 路由规范

1. **hash 模式** `createWebHashHistory()`，配合相对 base，使 dist 可部署到任意子目录。
2. 一业务模块一文件放 `router/modules/`，默认导出单个 `RouteRecordRaw`；`asyncRoutes.ts` 用 `import.meta.glob` 自动收集并按 `meta.order` 排序——**新增模块零改动**。
3. `Layout` 静态 import；页面统一懒加载 `component: () => import('@/views/xxx/index.vue')`。
4. 详情/编辑页用参数路由（`detail/:id`），并设 `hidden: true` + `activeMenu` 指回列表菜单。
5. 动态路由挂载完成后**必须最后追加兜底 404**（否则刷新时动态路由误落 404）。
6. `meta` 字段统一约定（在 `types/router.d.ts` 中 `declare module 'vue-router'` 扩展声明，未使用的字段不要声明）：

| 字段 | 语义 |
|---|---|
| `title` | 菜单/页面标题，守卫写入 `document.title` |
| `icon` | Element Plus 图标名字符串 |
| `perm` | 权限码；打在受控单元最高节点，子路由自动继承；无值即公开 |
| `hidden` | 侧边栏隐藏 |
| `keepAlive` | 页面缓存（要求路由 name 与组件 `defineOptions.name` 一致） |
| `order` | 菜单排序 |
| `activeMenu` | 子页高亮所属菜单 |

7. 守卫写在独立的 `router/guard.ts`，`main.ts` 副作用引入。标准逻辑：无 token → 白名单放行否则跳登录带 `redirect`；有 token 访问 `/login` → 回首页；首次进入拉取用户信息与授权、`addDynamicRoutes` 后 `return { ...to, replace: true }` 重进一次。

---

## 6. 状态管理规范

1. `store/index.ts` 只做 `createPinia()` 装配；**不装 pinia-plugin-persistedstate**，除非某 store 真的声明了 `persist`（历史两个项目都是装而不用）。登录 token 走 `utils/auth.ts` 手写 localStorage。
2. **store 统一用 Options 写法**（两个既有项目一致）：

```ts
export const useUserStore = defineStore('user', {
  state: () => ({
    token: getToken(),
    header: null as DashboardHeader | null, // 初值用 as 断言定型
  }),
  getters: { /* ... */ },
  actions: { /* ... */ },
})
```

3. **store 的职责边界：只放跨页面/跨组件共享的会话上下文**（用户信息、权限集、当前组织等）。页面/卡片自己的数据由页面自己取，不进 store。
4. 跨 store 调用直接在 action 里 `useXxxStore()`；登出/重置必须集中在一个 `resetState()` 里完成清 token、清动态路由、清权限。
5. 「切换上下文后全页刷新」用 **reloadFlag 自增 + 组件 `:key` 重挂载**模式（Board 端组织切换的现行做法），不做零散的手动重拉。

---

## 7. 网络请求规范

### 7.1 axios 单例（`utils/request.ts`）

所有系统统一以下五点，模板代码直接从既有项目复制：

1. **雪花 ID 精度安全**：`transformResponse` 用 `json-bigint({ storeAsString: true })` 替代原生 JSON.parse；所有 ID 字段类型为 `BigId = string | number`，传参时 `String(id)`。
2. **请求头**：每请求固定打 `X-App-Type: <本端标识>`；有 token 再打 `ENTERPRISETOKEN: <token>`（Sa-Token 头名与后端一致）。相关常量集中在 `src/settings.ts`。
3. **响应解包**：`code === 200` 直接返回 `res.data`；无 `code` 字段（文件流等）原样返回；`code ∈ [401, 4001]` 或 HTTP 401 → `handleNotLogin()`（清 token + 提示 + 跳登录带 redirect，用模块级布尔去重并发跳转）；其他业务码 → `ElMessage.error(res.msg)` 并 reject。
4. **错误提示统一在拦截器完成**。页面只写 `try/finally` 管 loading，不写 catch 弹提示；确需静默的探测/轮询请求用 `silent: true`，并通过模块增强合法化（不要 `as AxiosRequestConfig` 强转）：

```ts
declare module 'axios' {
  interface AxiosRequestConfig { silent?: boolean }
}
```

5. **对外出口**：泛型薄壳 `request.get/post/put/delete<T>`，`T` 即解包后的业务数据类型；需要原始 `AxiosResponse` 的场景（blob 导出）用 `export default service`。

### 7.2 API 模块组织

1. **按后端 Controller/领域一文件**（`api/enterprise.ts`、`api/tenant.ts`），不按页面拆。
2. 文件顶部模块级注释标对接文档章节；**每个函数一行 JSDoc 并标注文档编号**：

```ts
/**
 * 租户 / 组织管理（对接文档 第六章 /tenant）。
 * 组织以 tenantCode（字符串编码）为主键，区别于部门的数字 ID。
 */

/** 组织分页列表（6.1） */
export function pageTenant(params: PageQuery & { keyword?: string }) {
  return request.post<PageResult<TenantItem>>('/tenant/tenantPage', params)
}
```

3. 类型与函数**就近成对定义**在同一 api 文件；页面 `import type { EnterpriseItem } from '@/api/enterprise'`。跨模块通用类型（`ApiResult` / `PageQuery` / `PageResult`）放 `types/api.ts`；大型领域契约（如看板全量数据结构）可集中一个 `types/<domain>.ts`，文件头写清全局口径（金额单位、百分比语义、ID 精度）。
4. 查询参数接口继承 `PageQuery`；后端未穷举字段的实体用 `[key: string]: unknown` 索引签名兜底；数字枚举字段行内注释标明每个值的含义。
5. 参数清洗（`trim`、空转 `undefined`）写在 api 层，不散落在页面。
6. **禁止在代码中硬编码任何第三方 token/密钥**（历史违规：feedback 网关 token 写死在源码并进了生产包）。此类凭证走环境变量或后端代理。
7. 对接独立第三方网关时，允许在该 api 文件内自建独立 axios 实例并就近定义类型，不污染主请求单例。

---

## 8. 组件规范

### 8.1 基本写法

1. **一律 `<script setup lang="ts">`**，SFC 块顺序固定 `script → template → style`。
2. props 用**纯 TS 泛型声明** `defineProps<{ data: AreaDistribution }>()`；默认值用 `withDefaults`。
3. **emits 统一用具名元组语法**（规范决策，取 Board 端写法，Vue 3.3+ 官方推荐；Admin 端旧的调用签名式不再新增）：

```ts
const emit = defineEmits<{
  /** 新增成功，携带新记录 ID，供父级刷新列表 */
  success: [id: string]
  switchOrg: [orgId: BigId]
}>()
```

4. keepAlive 页面必须 `defineOptions({ name: '<与路由 name 相同>' })`。
5. 双向绑定用 `defineModel<boolean>({ required: true })`。

### 8.2 弹窗统一模式

管理侧弹窗统一「**ref + defineExpose(open) + emit('success')**」非受控模式：

```ts
// 子组件
const visible = ref(false)
function open(row?: XxxItem) { /* 回填 */ visible.value = true }
function handleClosed() { formRef.value?.resetFields() } // 绑 @closed
defineExpose({ open }) // 新增/编辑合体的弹窗暴露 openCreate / openEdit

// 父组件
const dialogRef = ref<InstanceType<typeof XxxDialog>>()
dialogRef.value?.open(row)
// <XxxDialog ref="dialogRef" @success="search" />
```

约定：`el-dialog` 一律 `:close-on-click-modal="false"`；header 统一「竖蓝条 + 标题」结构；提交套路固定为「validate → loading → try 调 api → ElMessage.success → 关窗 → emit('success') → finally 关 loading」。

### 8.3 表格页统一用 `useTable`

分页列表一律使用 `composables/useTable.ts`（泛型 `useTable<T, Q extends PageQuery>`，返回 `loading/list/query/currentPage/pageSize/total/getList/search/reset/handlePageChange/handleSizeChange`）；弹窗内的分页用 `immediate: false`，**不要另写手工 pager**。含 `show-overflow-tooltip` 列的表格统一挂 `settings.ts` 的 `TABLE_TOOLTIP_OPTIONS`。

keepAlive 列表页返回刷新的固定片段：

```ts
// 首次挂载 useTable 已请求过，仅在从详情页返回等再次激活时刷新
let activatedOnce = false
onActivated(() => { if (activatedOnce) getList(); else activatedOnce = true })
```

### 8.4 展示型卡片（看板/工作台类系统）

数据卡片三条铁律（Board 端模板化架构，后续大屏/工作台系统照搬）：

1. **卡片只接收 `data` 一个数据 prop，自己不取数**；取数逻辑由注册表的 `fetcher` 签名约定，装载器统一负责 loading/error/retry。
2. 卡片外壳统一 `CardShell`（title/caption），空态统一 `<div class="card-empty">暂无…</div>`。
3. 「组件 + fetcher + 布局配置」通过注册表（`Record<WidgetKey, WidgetMeta>` + `defineAsyncComponent` 懒加载）串联；**新增卡片 = 写组件 + 注册一行 + 布局配置引用**，未来切换为后端下发配置时渲染链路不变。

跨层级交互（下钻等）用 `InjectionKey` + provide/inject，inject 必须带空函数默认值，保证卡片可独立复用。

### 8.5 定时器/轮询

统一用 VueUse 的 `useIntervalFn`（自动随组件销毁清理）；确需手写 `setInterval/setTimeout` 的，必须成对写 `onUnmounted`（keepAlive 页还要 `onActivated/onDeactivated`）清理，`timer ??=` 保证幂等。同一项目内不允许三种定时器写法并存（历史坏味道）。

---

## 9. 样式规范

1. **SCSS 是唯一样式语言**，不引入原子化 CSS。
2. 三层全局样式，职责不许混放：
   - `variables.scss`：**只放变量**，色值取自 Figma 设计稿标注（品牌主蓝 `$color-primary: #2f6bff` 等），由 vite `additionalData` 自动注入，业务文件直接用 `$` 变量；
   - `index.scss`：基础 reset、字体栈、用 SCSS 变量覆盖 Element Plus CSS 变量（`--el-color-primary: #{$color-primary}` 及各色阶）、**通用工具类**（`.app-card`、`.card-title`、`.card-empty`、`.pager` 等）——凡是在 ≥2 个页面重复的样式块（如表头 `#f5f8fd` + 圆角的 `:deep(.el-table)` 调整、分页条）必须上移到这里，禁止逐页复制；
   - `element.scss`：Element Plus 全局调优，按组件分节，每节标 Figma 节点号。
3. 组件内样式默认 `<style scoped lang="scss">`；穿透用 `:deep()`。
4. **弹窗样式**（teleport 到 body，scoped 够不着）：用非 scoped `<style lang="scss">` 块 + 「唯一根 class + `.el-dialog` 提权」收口，如 `.create-enterprise-dialog.el-dialog { ... }`。**同一个 class 的全局样式只允许定义一次**；通用弹窗骨架统一放 `src/styles/dialog.scss`（`.app-dialog`），禁止跨模块引用别的模块 views 下的 scss、禁止依赖「恰好被其他组件加载过」的全局样式（历史真实 bug：深链进详情页时弹窗样式丢失）。
5. CSS 命名：语义化 kebab-case + 嵌套 + 状态类，不用 BEM；颜色统一现代写法 `rgb(31 66 135 / 5%)`。
6. **大屏适配**（看板类系统）：1920×1080 基准画布 + `transform: scale()` 等比缩放（不用 rem/vw），画布内按设计稿像素书写；配套两个必做处理：ECharts `devicePixelRatio` 放大防发虚、tooltip `appendTo: 'body'` 防被卡片 `overflow: hidden` 裁切。
7. 图标统一 Element Plus 图标体系；**禁止用 emoji 当界面图标**（跨平台/大屏一体机渲染差异不可控）。

---

## 10. 权限与登录

1. token 存 localStorage，key 按 §2.6 分配，读写只经 `utils/auth.ts`。
2. 权限模型（有权限体系的系统）：**静态路由表为结构源 + 后端授权码过滤 + 动态挂载**。过滤规则：节点有 `perm` 且未授权 → 整棵子树剔除；已授权 → 保留并用后端 title/icon/order 覆盖；无 `perm` → 公开；子节点全空 → 剪掉空分组。
3. 按钮级权限用 `v-perm` 指令（字符串或数组，任一命中即显示），权限码 `模块:动作`，与菜单同一套。
4. 401/4001 兜底在 axios 拦截器，与路由守卫双保险；登出接口失败也照常清 token 跳登录。
5. 无权限体系的系统（如看板）：数据可见范围由后端会话决定，前端不持久化组织/范围状态。

---

## 11. 工具函数与格式化

1. 纯函数进 `utils/`，一文件一主题（`format.ts` / `auth.ts` / `echarts.ts`），全部具名导出 + 一行 JSDoc，不用 default export。
2. **格式化统一收口到 `utils/format.ts`**，同一项目内禁止两处定义同名格式化函数（历史坏味道：两个 `formatDate` 行为不同）：
   - 数字：`formatInt` / `formatDecimal`，先 `Number()` 兜底，**非有限值统一返回 `'—'`（全角破折号，全项目唯一空值占位符）**，千分位用 `toLocaleString('en-US')`；
   - 日期：dayjs 或字符串裁剪均可，但同一语义只留一个函数。
3. ECharts 按需注册集中在 `utils/echarts.ts`，`main.ts` 副作用引入；图表组件统一 `<VChart :option="chartOption" autoresize />`，`chartOption` 一律 `computed`。
4. 健壮性习惯（必须保持）：模板与计算里对后端数据一律 `?.` / `?? []` / `Number(x) || 0` 兜底；进度条类数值做 `Math.min/Math.max` 夹取；后端枚举新增值要有 fallback。

---

## 12. 常量、类型与注释

1. 全局协议常量（token 头、业务码、白名单）集中 `src/settings.ts`；页面级选项常量就近定义在页面顶部，SCREAMING_SNAKE。
2. 业务枚举 = 字符串字面量联合类型 + `Record<联合类型, Meta>` 映射表 + `xxxMeta()/xxxText()` 取值函数；**同名映射表禁止在两个模块重复定义不同语义**（历史坏味道：两个 `TASK_STATUS_META`）。
3. 注释规范（团队现有最强实践，必须延续）：
   - 模块文件顶部块注释：职责 + 设计取舍；
   - 导出成员/接口字段用 `/** */`，标注单位、取值范围、空值语义；
   - 接口注释带**后端对接文档章节号**（`（对接文档 3.3）`），样式注释带 **Figma 节点号**（`（设计稿 16556:7790）`）；
   - 「为什么这么写」必须写（为何 hash 路由、为何 tooltip 挂 body 等）；
   - 空 `catch {}` 必须留一行注释说明为何忽略；
   - 未就绪项统一 `TODO(后端未就绪)`，后端弃用字段用 `@deprecated`。
4. 禁止残留 `console.*`（`noUnusedLocals` + lint 双重把关）。

---

## 13. Git 与协作

1. **提交信息强制 conventional commits**（`feat:` / `fix:` / `chore:` / `refactor:` / `docs:` + 中文描述）。历史项目出现连续多条「最新代码」提交，新项目一律配 **husky + commitlint + lint-staged**（pre-commit 跑 `eslint --fix`，commit-msg 校验格式），不靠口头约定。
2. 不提交：`dist/`、`dist.tar.gz`、`node_modules/`、`.env.*.local`。**要提交**：`auto-imports.d.ts`、`components.d.ts`、`pnpm-lock.yaml`。
3. README 必须包含：启动方式、环境变量说明、目录结构、以及「新增一个业务模块的标准步骤」（见 §14），并与代码保持同步。

---

## 14. 新增一个业务模块的标准步骤（管理后台类系统）

1. `src/api/<module>.ts`：定义 `XxxItem` / `XxxQuery extends PageQuery` + `pageXxx/getXxx/createXxx/...`，JSDoc 标文档章节号。
2. `src/router/modules/<module>.ts`：默认导出路由，顶级节点 `meta: { title, perm: '<module>:list', order }`，列表页 `keepAlive: true`，详情页 `hidden: true` + `activeMenu`。
3. `src/views/<module>/list/index.vue`：`defineOptions({ name })` + `useTable` + `.app-card` + `TABLE_TOOLTIP_OPTIONS` + `v-perm` 按钮 + 统一分页条。
4. 弹窗与页面同级：`XxxDialog.vue`，`defineExpose({ open })` + `emit('success')`，样式以 `.xxx-dialog.el-dialog` 收口（骨架复用 `styles/dialog.scss`）。
5. 跨页共享的枚举映射/格式化放 `views/<module>/shared.ts`。
6. 后端菜单权限管理登记同名 `perm` 并授权。

看板/大屏类系统新增卡片：写 `XxxCard.vue`（只接 `data` prop）→ `board/registry.ts` 登记一行（name/component/fetcher）→ 布局配置引用 WidgetKey。

---

## 15. 红线清单（Code Review 必查）

以下条目来自两个既有项目的真实问题，出现即打回：

- [ ] 装了不用的依赖/插件（引入前先回答「哪个文件会用它」）
- [ ] 源码中硬编码第三方 token、密钥、写死的外部网关地址
- [ ] 构建产物（dist、tar.gz）提交入库
- [ ] 提交信息不符合 conventional commits
- [ ] 路由 path 出现 camelCase
- [ ] 同名全局弹窗样式在多个文件重复定义；跨模块引用他模块 views 下的私有 scss
- [ ] 页面手写 pager/手写请求错误弹窗（应走 `useTable` / 拦截器）
- [ ] 同一项目出现两套空值占位符或两个行为不同的同名格式化函数
- [ ] 手写定时器不清理，或同项目多种定时器写法并存
- [ ] `as AxiosRequestConfig` 之类的强转绕过类型（该扩展就扩展）
- [ ] SFC 超 400 行未做拆分说明
- [ ] emoji 当界面图标
- [ ] `console.*` 残留、`any` 滥用（允许范围仅：request 泛型默认值、索引签名兜底、`catch (error: any)`）
- [ ] 新页面不带中文注释、接口函数不标对接文档章节号
