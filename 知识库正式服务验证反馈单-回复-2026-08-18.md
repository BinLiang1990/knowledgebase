# 统一知识库正式服务验证反馈单 · 回复

- 回复方：统一知识库
- 回复日期：2026-08-18
- 对应反馈单：《知识库正式服务验证反馈单》（贵方 2026-08-17）
- 契约依据：《经验库对接接口文档-约定基准版》（2026-08-14 双方确认版，下称"基准版"）
- 总体结论：**问题 1、问题 2 确认成立，属我方正式环境数据初始化遗漏，非契约变更，我方接受并处理，贵方零改动；问题 3 已查明根因，同意将库列表接口纳入免鉴权只读面；问题 4 确认 kb=1 为我方调试库。** 处理排期见文末汇总表。

---

## 一、问题 3 回复：`GET /knowledge-bases` 返回 401 与"预留 token"说明

### 1. 401 的根因

正式环境已于 2026-08 启用统一身份认证（SSO），运营页面的全部接口均要求登录态。基准版 §5 签字的只读面接口是按清单**逐条显式豁免**鉴权的——这正是贵方"验证通过项"中 §5 各接口均正常返回 200 的原因。

`GET /knowledge-bases`（库列表）**不在基准版 §5 签字清单内**，因此未列入豁免清单，被认证层按运营接口拦截，返回 401。该文案（"登录已过期，请重新登录"）是面向运营端登录场景的统一提示，对机器调用确有误导，请以本说明为准。

### 2. 关于"预留 token"（`Authorization: Bearer a8a6…`）

需要澄清：**我方 v1 阶段从未发放过任何 API token，服务端也不解析 `Authorization` 请求头**。基准版 §5 约定的正是只读面"无 API Key、无签名、无 Token"，与当前实现一致。贵方持有的 `a8a6…` 不在我方任何登记记录中，请贵方核实其来源（疑似其他系统的凭证或早期沟通遗留），避免继续误用——无论携带与否，结果相同。

另请贵方自查一种可能：统一身份平台已发布《服务 Token 接口对接文档》（2026-08-18），若该 token 是贵方从平台 `POST /core/service-auth/token` 申请的**服务 Token**，请注意——(a) 传递请求头应为 `X-Service-Token`，不是 `Authorization: Bearer`；(b) 我方目标系统侧的服务 Token 校验尚未上线，现阶段无论如何携带均不生效；(c) 申请时 `targetSystem` 应填我方系统编码 **`tyzsk`**（非示例中的 `knowledge`），否则校验必为 `active=false`。服务 Token 的正式启用即下文"机器凭证二期切换"，届时按"发凭证 → 联调 → 约定日期强制"过渡，另行书面约定排期。

### 3. 处理决定（对应贵方"请对方确认"项）

- **同意将 `GET /knowledge-bases` 纳入免鉴权只读面**，采用与 §5 其余接口完全一致的免鉴权方案。贵方零适配、无需任何 token，经验配置页可直接下拉选库。
- **不单独发放 token**：只读面的机器凭证将随统一身份平台的"服务间机器凭证"能力（二期）整体切换，届时整个只读面一起走"发凭证 → 联调 → 约定日期强制"的过渡流程，另行书面约定。现阶段不自建一次性 token 方案，避免贵方两次适配。
- 返回体说明：数组结构，每项含 `id`、`name`、`description`、`status`（`active`/`deprecated`）等字段；支持 `?status=active` 过滤，建议贵方下拉仅展示 `active` 的库。
- 契约动作：该接口将补入基准版 §5 只读面清单（作为 v1.1 修订），随本回复请贵方回签确认。
- 排期：改动为豁免清单扩充，风险低；**预计 2026-08-19 随问题 1 一并发布**。【发布日期如有调整将在通知渠道另行同步】

---

## 二、问题 1 回复：`tenant` 维度缺失

- **确认基准版 §3 继续有效，无契约变更意图**：租户隔离以英文 `tenant` 维度（text 型）表达；`行政区划` 是普通业务维度，不是、也不会被用作租户维度。
- 根因：正式环境的全局维度为数据初始化时按业务名录入，**遗漏了 `tenant` 维度的创建**，属我方数据配置遗漏，不是实现分歧。
- 处理：新建全局维度 `tenant`（key=`tenant`，text 型），并在对接涉及的知识库（kb=2/3/4，后续新增业务库同步执行）启用。此为纯配置操作，**2026-08-18 当日完成**。完成后贵方复现用例 `GET /knowledge-bases/2/knowledge-points?coord={"tenant":"测试"}` 将不再返回 400。

---

## 三、问题 2 回复：存量数据补写租户条件

确认问题成立：kb=2/3/4 合计 117 个知识点的存量答案未携带租户条件，接口层修复后数据层隔离仍不成立。按贵方所列三项处理：

1. **启用 `tenant` 维度**：随问题 1 于 2026-08-18 完成（原已启用的 `分类` 维度保持不变）。
2. **存量归属盘点**：我方业务侧对 117 个知识点逐条确认归属——属特定租户的补写 `tenant` 条件；确属全局共享的列入**全局经验白名单**，白名单清单随盘点结果同步贵方备案（即第四部分第 2 项）。**暂定 2026-08-21 前给出盘点结果与白名单清单**。【盘点完成时间以业务侧确认为准】
3. **补写实施与核验**：我方以数据层统一迁移方式为归属租户的答案补写 `tenant` 条件（不走逐条编辑接口，避免在版本链上产生额外的撤回/迁移噪音，保持贵方通过 `change-log`/`answer-groups` 看到的历史干净）。完成后通知贵方，请按基准版 §12.3 跑 tenant 巡检脚本核验。**暂定 2026-08-25 前完成补写**。【以盘点完成时间顺延】

**隔离语义主动确认**（供贵方核验时对照）：补写完成后，携带非空 `tenant` 的查询只会命中两类答案——(a) 同 `tenant` 取值的答案（`exact` 或 `weighted`）；(b) 全局经验白名单内、不写任何条件（`coord={}`）的答案（`weighted` 命中，这正是"全局共享"的既定机制）。**不会命中其他租户的答案**；基准版 §6 的 `fallback-latest` 回退仅在查询条件为空时才可能出现，贵方"所有读取强制携带非空 `tenant`"的红线可将其完全规避。

---

## 四、问题 4 回复：kb=1 测试数据

确认 kb=1 为我方内部调试库（"苹果""梨"为功能验证数据），非业务库。我方将安排清理/停用；在此之前请贵方对接配置避开 kb=1。业务对接仅涉及 kb=2/3/4。

---

## 五、贵方所需信息

### 1. 三个业务库的 kb_id 对应关系

| 贵方名称 | kb_id | 我方库名（基准版 §3 建模名） | 知识点数 | 内容摘要（供核对） |
| --- | --- | --- | --- | --- |
| 打标经验库 | **2** | 全局打标经验（`tagging`） | 86 | 打标经验条目，如"资质荣誉类标签应独立打标"（与贵方观测一致） |
| AI 小助手规则库 | **3** | 打标助手规则库（`tagging_skill`，小助手播报规则） | 23 | 7 大动态维度定义与触发规则、任务说明等 |
| 运营技能库 | **4** | 操作助手规则库（`operate_skill`，小助手操作规则） | 8 | 助手错误提示对照表、异常文案等操作规则 |

kb=2 与贵方观测一致；**kb=3 / kb=4 的名称对应请贵方业务侧按"内容摘要"列核对后在回执中确认**，若与贵方内部命名不符请指出，以内容为准。

### 2. 全局经验白名单清单

随问题 2 第 2 步盘点结果一并提供（暂定 2026-08-21 前），提供后请贵方备案。

### 3. 变更窗口 / 发布通知渠道

【待填：请补充我方约定的通知渠道与变更窗口，例如"发布前一个工作日在双方对接群通知；变更窗口为工作日 19:00 后"】

---

## 六、处理排期汇总

| 事项 | 对应问题 | 暂定完成时间 |
| --- | --- | --- |
| 新建 `tenant` 全局维度并在 kb=2/3/4 启用 | 问题 1 | 2026-08-18 |
| `GET /knowledge-bases` 纳入免鉴权只读面并发布 | 问题 3 | 2026-08-19 |
| 存量 117 点归属盘点 + 全局经验白名单清单 | 问题 2 / 信息 2 | 2026-08-21 前 |
| 存量答案补写 `tenant` 条件，通知贵方巡检核验 | 问题 2 | 2026-08-25 前 |
| kb=1 调试库清理/停用 | 问题 4 | 随上述发布一并安排 |
| 基准版 §5 v1.1 修订（补入库列表接口） | 问题 3 | 随本回复发出，待贵方回签 |

## 七、需贵方配合的动作

1. 回签基准版 §5 v1.1 修订（新增 `GET /knowledge-bases` 至只读面清单）；
2. 核实 `a8a6…` token 的来源，并从对接配置中移除该凭证；
3. 按第五部分第 1 项核对并回执 kb_id 对应关系；
4. 问题 1/2 完成通知后，重跑贵方验证用例并执行基准版 §12.3 的 tenant 巡检，将结果同步我方。
