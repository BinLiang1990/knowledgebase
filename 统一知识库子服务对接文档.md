# 统一知识库（tyzsk）· 子服务对接文档

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0（2026-08-18） |
| 适用对象 | 需要调用统一知识库的**其他子服务/业务系统**（服务器到服务器的机器调用） |
| 系统编码 | `tyzsk`（统一身份平台登记编码，申请服务 Token 时 `targetSystem` 必须填此值） |
| 服务地址 | 【待填：按环境提供，测试/生产各一个；无统一路径前缀，接口即 `GET /knowledge-bases`，不是 `/api/v1/...`】 |
| 在线文档 | 部署地址下的 `/docs`（Swagger）、`/redoc`、`/openapi.json`，公开可访问，联调时可直接对照 |
| 冲突处理 | 本文档依据服务端代码（`backend/src/kb_backend`）逐条核对生成；如与代码/Swagger 冲突，以代码为准并反馈我方更新本文档 |
| 相关文档 | 统一身份平台《服务 Token 接口对接文档》（2026-08-18）；《知识库服务对接接口文档-约定基准版》（bqxt 专项契约，本文档与其 §5 接口面同源） |

**一句话概括**：统一知识库是一个"按维度条件查询知识点答案"的服务。子服务给出"知识库 + 知识点 + 一组`维度=取值`条件 + 查询时间点"，服务返回该条件下命中的那条答案。对子服务开放的是**只读接口面**；数据的增删改由知识库运营侧维护，不对子服务的机器调用开放。

---

## 1. 鉴权（先读这一章）

系统入口网关按"先系统侧、后用户侧"识别凭证，共三种通行方式：

| 通行方式 | 凭证 | 适用方 | 可访问面 |
|---|---|---|---|
| 公开 | 无 | 任何调用方 | `/health`、`/docs`、`/redoc`、`/openapi.json` |
| **服务间机器凭证** | 请求头 `X-Service-Token` | **子服务（本文档读者）** | §3 只读接口面（按来源系统白名单，仅 GET） |
| 用户登录态 | 请求头 `IDENTITYTOKEN` | 运营页面的登录用户 | 按角色（viewer/editor/admin/sysadmin），子服务不使用 |

### 1.1 现阶段：只读面过渡期免鉴权

**当前 §3 列出的只读接口面处于过渡期免鉴权状态**——不带任何凭证即可调用，与历史约定保持一致（对已接入方零适配）。

过渡期结束后（按"发凭证 → 双方联调 → 约定日期强制"流程，**书面另行约定日期，不一刀切**），只读面将强制校验服务 Token：届时未携带有效 `X-Service-Token` 的调用返回 401，`msg` 为 `本接口需携带服务凭证（X-Service-Token）`。**新接入的子服务请直接按服务 Token 方式开发**，不要依赖免鉴权现状。

### 1.2 服务 Token：申请与携带

服务 Token 由**统一身份平台**签发，不由知识库签发。完整签名细节见平台《服务 Token 接口对接文档》，要点：

1. **前置登记**：你方系统须先在统一身份平台登记为业务系统并配置客户端密钥（HMAC 密钥）；
2. **获取 Token**：用你方客户端密钥按平台的六段式 HMAC-SHA256 签名规则调用：

   ```
   POST {统一身份平台}/core/service-auth/token
   { "sourceSystem": "<你方系统编码>", "targetSystem": "tyzsk" }
   ```

   ⚠️ `targetSystem` 必须填 **`tyzsk`**（平台文档示例中的 `knowledge` 只是示例值）。填错时 Token 能签发，但知识库侧校验必为 `active=false`，全部请求 401；
3. **携带调用**：业务请求加请求头（注意**不是** `Authorization: Bearer`，服务端不解析 `Authorization` 头）：

   ```http
   X-Service-Token: <serviceToken>
   ```

4. **缓存与刷新**：Token 有效期 7200 秒。请只在服务进程内存中缓存 `serviceToken` + `expiresAt`，到期前主动重取；业务调用收到 401 时强制重取一次并**仅重试一次**；
5. **不要混用**：同一请求**不得同时携带** `X-Service-Token` 和 `IDENTITYTOKEN`，否则直接 400 `请求不能同时携带用户 Token 与服务 Token`。

### 1.3 知识库侧的校验规则（网关行为）

携带 `X-Service-Token` 的请求，知识库网关按以下顺序处理：

1. 调统一身份平台校验接口验证 Token（结果有进程内 TTL 缓存，缓存期不超过 Token 剩余有效期）；**平台不可达或结果不确定时一律拒绝（fail-closed），不降级放行**；
2. 仅当 `active=true` 且 Token 的 `targetSystem == tyzsk` 时接受身份，否则 401 `服务凭证无效或已过期`；
3. **来源系统接口白名单**：统一身份平台不维护系统间授权关系，"能签出 Token"不等于"能访问接口"。知识库侧按 `sourceSystem` 维护每个来源系统的可访问接口面（当前均为 §3 只读面，仅 GET/HEAD）。来源系统未在我方登记、或访问白名单外的接口，返回 403 `该来源系统无权访问此接口`。

因此**接入前必须完成一步登记动作**：把你方系统编码告知我方，由我方把你方加入来源系统白名单并确认可访问面。流程见 §10。

### 1.4 鉴权相关错误速查

| HTTP | `msg` | 原因与处理 |
|---|---:|---|
| 400 | `请求不能同时携带用户 Token 与服务 Token` | 去掉其中一个凭证头 |
| 401 | `服务凭证无效或已过期` | Token 过期/伪造/`targetSystem` 不是 `tyzsk`/来源或目标系统在平台已停用。重取 Token 后仅重试一次 |
| 401 | `本接口需携带服务凭证（X-Service-Token）` | 免鉴权过渡期已结束而你方未带 Token——按 §1.2 接入 |
| 401 | `登录已过期，请重新登录` | 调用了只读面**之外**的接口且无凭证。该文案面向运营登录场景；机器调用见到它，说明调了约定外的接口 |
| 403 | `该来源系统无权访问此接口` | Token 有效但接口不在你方白名单内（例如调了写接口）。写接口不对机器调用开放 |

---

## 2. 调用约定

- 所有响应统一包一层信封：

  ```json
  { "code": 200, "data": { }, "msg": "操作成功" }
  ```

  - `code` 只有 `200`（成功）/ `444`（失败）两个值，与 HTTP 状态码是**两套独立信号**（即使 HTTP 是 400/404/422/500，body 的 `code` 也固定 444）；
  - **程序化分支只依赖 HTTP 状态码**：400=参数/业务错误，401/403=鉴权（§1.4），404=资源不存在，422=请求格式错误，500=内部错误；`msg` 是可读中文，仅透传展示/记日志；
  - 没有枚举型 `error_code`；错误文案变更会提前书面通知，不会不打招呼改文案；
  - 成功的列表接口 `data` 直接是数组，**没有分页**（见 §9）。
- **超时与重试**：只读面全部是纯读、无副作用，可任意重试。客户端超时建议初值：`resolve` 3 秒；全量列表/change-log 10 秒。500 `database unavailable` 类错误可重试。服务端当前无限流，请你方自行控制并发；
- 建议保留降级兜底：知识库异常时业务按"无知识/无经验"路径继续，不阻塞你方主流程。

---

## 3. 对子服务开放的接口面（全集）

以下 7 组 GET 接口就是机器凭证可访问面的**全集**，白名单按此配置。此外无其他接口对子服务开放。

| # | 接口 | 用途 |
|---|---|---|
| 3.1 | `GET /knowledge-bases` | 知识库列表（选库/下拉） |
| 3.2 | `GET /dimensions`、`GET /knowledge-bases/{kb_id}/enabled-dimensions` | 维度定义：能用哪些条件查 |
| 3.3 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve` | **单点条件查询（核心接口）** |
| 3.4 | `GET /knowledge-bases/{kb_id}/knowledge-points` | 知识点列表 + 条件筛选（全量拉取） |
| 3.5 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answer-groups` | 答案分组树（审计/巡检） |
| 3.6 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/change-log`、`GET /change-log` | 变更留痕 |
| 3.7 | `GET /knowledge-bases/{kb_id}/stats` | 统计（对账用） |

### 3.1 知识库列表

```
GET /knowledge-bases?status=active
```

`status` 可选 `active`/`deprecated`，不传返回全部；**建议固定传 `active`**。响应 `data` 为数组，每项：

```json
{
  "id": 2,
  "name": "全局打标经验",
  "description": null,
  "status": "active",
  "active_knowledge_point_count": 86,
  "created_at": "2026-08-01T10:00:00",
  "updated_at": "2026-08-05T09:00:00"
}
```

注意：`kb_id` 是环境相关的自增整数，测试与生产必然不同，请**按环境分别配置**、不要写死跨环境复用。当前生产业务库为 kb=2（全局打标经验）/ kb=3（打标助手规则库）/ kb=4（操作助手规则库）；kb=1 为我方内部调试库，**请勿对接**（将清理/停用）。

### 3.2 维度：先确认能查什么条件

```
GET /dimensions                                        （全局维度定义）
GET /knowledge-bases/{kb_id}/enabled-dimensions        （该库启用的维度）
```

两者结构相同，均只返回全局 `active` 的维度：

```json
[ { "key": "tenant", "label": "tenant", "field_type": "text", "weight": 60 } ]
```

查询某库前**先调后者**确认该库可用哪些维度——查询条件 `coord` 里出现该库未启用的 key 会被 400 拒绝，不做静默忽略。`key` 即展示名，创建后不可变；`field_type` 为 `text`/`number`/`date`/`boolean`。

### 3.3 单点条件查询 resolve（核心接口）

已知知识点、想问"当前条件下答案是什么"时直接用它，不需要先拉列表再自己筛：

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve?at=2026-08-18&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `at` | 今天（服务端时区，UTC+8） | 查询时间点，`YYYY-MM-DD` |
| `coord` | `{}` | 条件组合，JSON 对象 URL-encode 后放查询串，如 `coord={"tenant":"A","客户等级":"VIP"}` |

响应 `data`：

```json
{
  "status": "exact",
  "answer": {
    "id": 55,
    "knowledge_base_id": 2,
    "knowledge_point_id": 12,
    "coord": { "tenant": "A" },
    "content": "……答案全文……",
    "effective_time": "2026-08-01",
    "note": null,
    "revoked": false,
    "operator": "张三",
    "source": "manual",
    "created_at": "2026-08-01T10:00:00"
  }
}
```

- `status` 五种取值：`exact`（条件精确命中）/ `weighted`（按权重回退命中）/ `default`（空条件查询命中默认答案）/ `fallback-latest`（空条件且无默认答案时兜底取最新，**带非空条件查询不会出现**）/ `none`（无兼容答案，`answer` 为 `null`）。除 `none` 外都表示"有答案"，区别只是命中方式；
- 知识点不存在 → 404 `知识点不存在`；知识点已被软删除 → **200** + `{"status":"none","answer":null}`（视为无答案，不是错误）；
- `content` 是**全文**，无长度上限、无摘要截断。

### 3.4 知识点列表 + 条件筛选（全量拉取）

```
GET /knowledge-bases/{kb_id}/knowledge-points?status=active&keyword=退货&at=2026-08-18&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `status` | `active` | `active` / `deleted` |
| `keyword` | 无 | **仅标题**模糊匹配（大小写不敏感，≤255 字符）；不支持按内容/备注搜索 |
| `at` / `coord` | 同 3.3 | 不传 `coord` 时返回全部匹配知识点，各自带解析预览；**传了非空 `coord` 后，解析结果为 `none` 的知识点整条排除**，不出现在列表里 |

每项在知识点字段基础上多带 `resolved` 字段（结构与 3.3 的响应完全相同，`answer.content` 同样是全文）——**按条件全量拉取一次列表调用即可拿到全部生效答案含全文，不存在 N+1**。

### 3.5 答案分组树（审计/巡检用）

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answer-groups?at=2026-08-18
```

返回该知识点**全部**条件组合分组（含已撤回的，标 `revoked: true`），每组带 `version_count`、`latest_answer`、`live_answer`（`at` 时点无生效版本则为 `null`）。展示"这个知识点历史上配置过哪些条件"用，不做筛选、不参与解析。

### 3.6 变更留痕

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/change-log    （单知识点）
GET /change-log                                                     （全局，跨库）
```

写操作时间线（创建/编辑/撤回），每条带 `before_content`/`after_content`；全局版每条额外带 `knowledge_base_id`/`knowledge_base_name`/`knowledge_point_title`。只读审计视图，不受知识点软删除/知识库停用影响。行内 `status`：`live`（链当前生效版本）/ `superseded`（被更晚版本取代）/ `revoked`（链已撤回）。

**全局 `/change-log` 无分页、全量返回**——核对场景优先用单知识点接口定点查，全局接口仅审计用并自行截断。

### 3.7 统计

```
GET /knowledge-bases/{kb_id}/stats
```

`{ "subject_count", "active_answer_count", "enabled_dimension_count", "today_change_count" }`，只统计 `active` 知识点范围。用于数据交接对账（条数核对 + 抽样 resolve 比对全文）。

---

## 4. 核心概念

| 概念 | 说明 |
|---|---|
| 知识库 | 顶层容器，`status` 为 `active`/`deprecated`。知识点、答案归属唯一知识库；**不支持跨库检索** |
| 维度 | 全局定义的条件字段（如 `tenant`、`地区`）。`key`（即展示名，不可改）、`field_type`、`weight`（1~100，权重回退用） |
| 知识库启用维度 | 每库从全局维度勾选子集；只有本库启用且全局 `active` 的维度能出现在查询条件里 |
| 知识点 | 可被问到的"问题"，同库内 `title` 唯一；`status` 为 `active`/`deleted`（软删除，可恢复，**kp_id 永不复用、恢复后不变**） |
| 答案 | 知识点在某条件组合下的具体说法：`content`、`effective_time`、`coord`、`revoked`。**不同 `coord` 的答案彼此独立并存**，互不覆盖 |
| 条件组合 coord | `{维度key: 取值}` 字典；`{}` 即默认答案。相同 coord 的答案构成一条**版本链** |
| 版本链 | 编辑 = 链尾追加新版本，历史版本永久只读保留，无物理删除 |
| 撤回 | 对整条版本链的逻辑删除（原因必填）；撤回后该条件下查不到结果。**撤回不是终态**：链上写入新版本会恢复整条链 |
| 解析 resolve | 给定"库 + 知识点 + 时间 + 条件"，按 §5 算法算出返回哪条答案 |

---

## 5. 解析算法（`status` 怎么来的）

对一个知识点，在给定 `at` + 查询条件 `Q` 下：

1. 只考虑 `effective_time <= at` 且未撤回的答案版本；同一 `coord` 内按 `(effective_time, created_at, id)` 取最大者为该组当前版本（同日多次编辑，最后一次胜出）；
2. **`Q` 为空**：有 `coord={}` 默认组 → `default`；否则取全部组里最新一条 → `fallback-latest`；
3. **`Q` 非空**：筛"条件兼容"的组——该组自己写明的每个维度取值都必须与 `Q` 一致；`Q` 问到但组没写的维度不参与比较（因此 **`coord={}` 组永远兼容**，充当最低优先级候选）：
   - 无兼容候选 → `none`；
   - 有候选：按 `(涉及维度数, 涉及维度权重和, effective_time, created_at, id)` 取最大；
   - 结果条件与 `Q` 完全一致 → `exact`；否则 → `weighted`（权重回退命中，展示/使用时务必与精确命中区分）。

条件之间固定"且"关系，**不支持"或"**；数值/日期维度只做精确相等匹配，不支持区间/大于/小于。

`coord` 取值按维度 `field_type` 严格校验（写入与查询同一套规则）：

| `field_type` | 允许取值 | 说明 |
|---|---|---|
| `text` | 字符串 | 会 strip；去空格后为空串等价于"没写这个条件" |
| `number` | JSON 数字或数字字符串 | 超过 2^53 的整数请以字符串传入保留精度；超精度浮点直接 400 |
| `date` | `"YYYY-MM-DD"` | 必须是合法 ISO 日期，不接受带时间 |
| `boolean` | JSON `true`/`false` | **严格类型**，字符串 `"true"` 会被拒绝 |

---

## 6. 租户（tenant）约定

- 租户隔离用全局 `tenant` 维度（text 型）表达：单租户知识 `coord={"tenant":"<租户标识>"}`，全局共享知识 `coord={}`；
- **强烈建议子服务所有读取强制携带非空 `tenant` 条件**。这样：
  - 只会命中两类答案——同 `tenant` 取值的答案（`exact`/`weighted`），以及全局共享白名单内 `coord={}` 的答案（`weighted`）；**不会命中其他租户的答案**；
  - `fallback-latest` 状态被完全规避（它只在空条件查询时出现）；
- 系统**没有"维度必填"校验**——录入侧漏写 `tenant` 的答案对所有租户查询都是兼容候选。此风险由我方录入规范 + 每周 tenant 巡检兜底；哪些知识点允许 `coord={}`（全局共享白名单）由双方确认后备案。

---

## 7. 答案对象字段承诺

resolve、列表 `resolved`、分组树中的答案对象为同一结构，固定携带以下字段，**只增不删**；读接口字段与行为变更提前通知：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | int | 答案版本 ID；编辑=追加新版本，`id` 随之变化 |
| `knowledge_base_id` / `knowledge_point_id` | int | 必选；引用锚点建议用 `knowledge_point_id` + `coord`（kp_id 永不复用） |
| `coord` / `coord_hash` | object / string | 条件组合及其规范化哈希 |
| `content` | string | **全文**，无长度上限 |
| `effective_time` | date | 生效日期（日期粒度，闭区间 `<= at` 即生效） |
| `note` | string\|null | 版本级备注 |
| `revoked` / `revoked_at` / `revoked_by` / `revoke_reason` | — | 撤回状态与留痕（注意：链被恢复时整链清空，历史撤回不可追溯，为已知缺陷） |
| `operator` / `source` / `created_at` | — | 操作与来源留痕（`operator` 为操作人真实姓名；2026-08-14 前的历史数据固定 `admin`） |

时间口径：`created_at` 等为不带时区的 naive datetime，服务端时区 UTC+8；`effective_time`/`at`/日期型维度取值为纯日期 `"YYYY-MM-DD"`。"当天录入、`effective_time` 填当天"即时生效；变更**无主动通知/回调**，请以你方缓存 TTL 收敛（建议分钟级）。

---

## 8. 错误表（业务部分）

鉴权类错误见 §1.4。只读面实际可能遇到的业务错误：

| HTTP | `msg` | 触发场景 |
|---|---|---|
| 400 | `维度 X 未在本知识库启用` | 查询 `coord` 用了该库未启用的维度 key |
| 400 | `维度 X 取值类型错误，应为...` / `...精度超出...范围` | `coord` 取值类型/精度不满足维度 `field_type`（§5 表） |
| 400 | `coord 参数必须是一个 JSON 对象` | `coord` 查询串解析后不是对象（如传了数组） |
| 404 | `知识库不存在` / `知识点不存在` / `答案不存在` / `维度不存在` | 路径 ID/key 找不到资源 |
| 422 | `请求参数校验失败` | 查询参数格式不对（如 `coord` 不是合法 JSON）；字段级细节只记服务端日志，不回传 |
| 500 | `内部错误` / `database unavailable` | 服务端异常/数据库不可用，**可重试** |

---

## 9. 已知限制（对接前确认可接受）

| 限制 | 说明 | 建议 |
|---|---|---|
| 无分页 | 列表/维度/分组树/change-log 全量返回 | 按"全量拉取 + 本地缓存"设计 |
| 无增量、无回调 | 无按时间过滤、无变更推送 | 缓存 TTL 自然收敛；核对用单知识点 change-log |
| 无跨库检索 | 所有查询限单个 `kb_id` | 按库分别拉取 |
| 无"或"条件、无区间匹配 | 条件固定"且"，数值/日期只做相等 | 复杂筛选在你方本地做 |
| 无限流、无 SLA 机制 | 服务端当前不限流 | 你方自行控并发 + 保留降级兜底 |
| keyword 仅搜标题 | 不支持内容/备注搜索 | 需要全文检索请在你方本地建索引 |
| 撤回留痕缺陷 | 已恢复链的历史撤回不可追溯 | 已登记为已知缺陷，待版本修复 |

---

## 10. 接入流程（新子服务 checklist）

1. **系统登记**：确认你方系统已在统一身份平台登记（有系统编码 + 客户端密钥）；未登记先走平台流程；
2. **知识库侧登记**：将你方**系统编码**与用途告知知识库方，我方将你方加入来源系统白名单（可访问面 = §3 只读面）并回复确认；
3. **拿地址**：向我方索取测试/生产环境服务地址；
4. **联调（测试环境）**：
   - 按 §1.2 从平台申请服务 Token（`targetSystem=tyzsk`），带 `X-Service-Token` 调 `GET /knowledge-bases` 验证 200；
   - 调 `GET /knowledge-bases/{kb_id}/enabled-dimensions` 确认可用维度；
   - 按你方业务跑通 resolve / 列表拉取，覆盖 `exact`/`weighted`/`none` 三种状态的处理；
   - 验证 Token 过期重取、401 单次重试、降级兜底逻辑；
5. **kb_id 配置**：按环境分别配置 kb_id（测试/生产不同），并与我方核对库名对应关系；
6. **上线**：与我方确认生产网络边界（内网/白名单）后切生产地址。

联系方式 / 变更通知渠道：【待填：对接群 / 邮件，发布前一个工作日通知】

---

## 附录：写接口简表（不对子服务开放）

知识点/答案的增删改（创建、批量导入、编辑、撤回、软删除等）由知识库运营侧在管理端操作，需用户登录态（`IDENTITYTOKEN` + 相应角色），**服务 Token 白名单不包含任何写接口**——机器调用写接口会得到 403。子服务如有录入需求，请通过运营渠道提交（常规 T+1 个工作日生效，紧急纠错当日响应）。
