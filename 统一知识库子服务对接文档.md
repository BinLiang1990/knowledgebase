# 统一知识库（tyzsk）· 子服务对接文档

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.3（2026-08-21）；变更记录见文末 |
| 适用对象 | 需要调用统一知识库的**其他子服务/业务系统**（服务器到服务器的机器调用） |
| 系统编码 | `tyzsk`（统一身份平台登记编码，申请服务 Token 时 `targetSystem` 必须填此值） |
| 服务地址 | 【待填：按环境提供，测试/生产各一个；无统一路径前缀，接口即 `GET /knowledge-bases`，不是 `/api/v1/...`】 |
| 在线文档 | 部署地址下的 `/docs`（Swagger）、`/redoc`、`/openapi.json`，公开可访问，联调时可直接对照 |
| 冲突处理 | 本文档依据服务端代码（`backend/src/kb_backend`）逐条核对生成；如与代码/Swagger 冲突，以代码为准并反馈我方更新本文档 |
| 相关文档 | 统一身份平台《服务 Token 接口对接文档》（2026-08-18）；《知识库服务对接接口文档-约定基准版》（bqxt 专项契约，本文档 §3 读接口面与其 §5 同源） |

**一句话概括**：统一知识库是一个"按维度条件查询知识点答案"的服务。子服务给出"知识库 + 知识点 + 一组`维度=取值`条件 + 查询时间点"，服务返回该条件下命中的那条答案。对子服务开放的接口面为：**维度只读**（维度的定义与配置由知识库运营侧维护）；**知识库管理接口**（本系统没有删除知识库的能力，"下线"即停用）；**知识点的全部增删改查**；以及**知识点下答案的写入**（新增、编辑、设为默认、撤回）。

---

## 1. 鉴权（先读这一章）

系统入口网关按"先系统侧、后用户侧"识别凭证，共三种通行方式：

| 通行方式 | 凭证 | 适用方 | 可访问面 |
|---|---|---|---|
| 公开 | 无 | 任何调用方 | `/health`、`/docs`、`/redoc`、`/openapi.json` |
| **服务间机器凭证** | 请求头 `X-Service-Token` | **子服务（本文档读者）** | §3 读接口面 + §4 写接口面（按来源系统白名单逐条配置） |
| 用户登录态 | 请求头 `IDENTITYTOKEN` | 运营页面的登录用户 | 按角色（viewer/editor/admin/sysadmin），子服务不使用 |

### 1.1 现阶段：读接口面过渡期免鉴权，写接口一律要求服务 Token

**§3 列出的读接口面处于过渡期免鉴权状态**——不带任何凭证即可调用，与历史约定保持一致（对已接入方零适配）。

过渡期结束后（按"发凭证 → 双方联调 → 约定日期强制"流程，**书面另行约定日期，不一刀切**），读接口面将强制校验服务 Token：届时未携带有效 `X-Service-Token` 的调用返回 401，`msg` 为 `本接口需携带服务凭证（X-Service-Token）`。**新接入的子服务请直接按服务 Token 方式开发**，不要依赖免鉴权现状。

**§4 写接口面从第一天起就必须携带有效的 `X-Service-Token`，不进免鉴权豁免**——写操作要落审计留痕（操作人按来源系统记录，见 §4.0），匿名写入不被接受。

### 1.2 服务 Token：申请与携带

服务 Token 由**统一身份平台**签发，不由知识库签发。完整签名细节见平台《服务 Token 接口对接文档》，要点：

1. **前置登记**：你方系统须先在统一身份平台登记为业务系统并配置客户端密钥（HMAC 密钥）；
2. **获取 Token**：用你方客户端密钥按平台的六段式 HMAC-SHA256 签名规则调用：

   ```
   POST {统一身份平台}/core/service-auth/token
   { "sourceSystem": "<你方系统编码>", "targetSystem": "tyzsk" }
   ```

   ⚠️ `targetSystem` 必须填 **`tyzsk`**（平台文档示例中的 `knowledge` 只是示例值）。填错时 Token 能签发，但知识库侧校验必为 `active=false`，全部请求 401；
3. **携带调用**：业务请求加请求头（注意**不是** `Authorization: Bearer`，服务端不解析 `Authorization` 头）：

   ```http
   X-Service-Token: <serviceToken>
   ```

   **写接口（POST/PATCH/PUT）还必须额外携带操作人头**，用于审计留痕（变更日志、答案历史里的 `operator` 字段会记为 `你方系统编码:操作人`）：

   ```http
   X-Service-Operator: <操作人姓名或账号>
   ```

   操作人含中文时请 **URL 编码**（如 `张三` → `%E5%BC%A0%E4%B8%89`）。缺此头的写请求直接 400。只读请求可不带（带了同样会记录）；

4. **缓存与刷新**：Token 有效期 7200 秒。请只在服务进程内存中缓存 `serviceToken` + `expiresAt`，到期前主动重取；业务调用收到 401 时强制重取一次并**仅重试一次**；
5. **不要混用**：同一请求**不得同时携带** `X-Service-Token` 和 `IDENTITYTOKEN`，否则直接 400 `请求不能同时携带用户 Token 与服务 Token`。

### 1.3 知识库侧的校验规则（网关行为）

携带 `X-Service-Token` 的请求，知识库网关按以下顺序处理：

1. 调统一身份平台校验接口验证 Token（结果有进程内 TTL 缓存，缓存期不超过 Token 剩余有效期）；**平台不可达或结果不确定时一律拒绝（fail-closed），不降级放行**；
2. 仅当 `active=true` 且 Token 的 `targetSystem == tyzsk` 时接受身份，否则 401 `服务凭证无效或已过期`；
3. **来源系统接口白名单**：统一身份平台不维护系统间授权关系，"能签出 Token"不等于"能访问接口"。知识库侧按 `sourceSystem` 维护每个来源系统的可访问接口面（在 §3 + §4 的全集范围内逐系统配置——**是否开放写接口、开放到哪些库，接入登记时逐条确认**）。来源系统未在我方登记、或访问白名单外的接口，返回 403 `该来源系统无权访问此接口`。

因此**接入前必须完成一步登记动作**：把你方系统编码与所需接口面（只读 / 读写）告知我方，由我方把你方加入来源系统白名单并回复确认。流程见 §11。

### 1.4 鉴权相关错误速查

| HTTP | `msg` | 原因与处理 |
|---|---:|---|
| 400 | `请求不能同时携带用户 Token 与服务 Token` | 去掉其中一个凭证头 |
| 400 | `服务凭证的写操作必须携带 X-Service-Operator 请求头…` | 写接口漏带操作人头——按 §1.2 第 3 条补上（中文需 URL 编码） |
| 401 | `服务凭证无效或已过期` | Token 过期/伪造/`targetSystem` 不是 `tyzsk`/来源或目标系统在平台已停用。重取 Token 后仅重试一次 |
| 401 | `本接口需携带服务凭证（X-Service-Token）` | 免鉴权过渡期已结束而你方未带 Token——按 §1.2 接入 |
| 401 | `登录已过期，请重新登录` | 调用了对接面**之外**的接口且无凭证。该文案面向运营登录场景；机器调用见到它，说明调了约定外的接口 |
| 403 | `该来源系统无权访问此接口` | Token 有效但接口不在你方白名单内（例如白名单只配了只读面却调了写接口，或调了 §5 不开放清单里的接口） |

---

## 2. 调用约定

- 所有响应统一包一层信封：

  ```json
  { "code": 200, "data": { }, "msg": "操作成功" }
  ```

  - `code` 只有 `200`（成功）/ `444`（失败）两个值，与 HTTP 状态码是**两套独立信号**（即使 HTTP 是 400/404/422/500，body 的 `code` 也固定 444）；
  - **程序化分支只依赖 HTTP 状态码**：400=参数/业务错误，401/403=鉴权（§1.4），404=资源不存在，422=请求格式错误，500=内部错误；`msg` 是可读中文，仅透传展示/记日志；
  - 没有枚举型 `error_code`；错误文案变更会提前书面通知，不会不打招呼改文案；
  - 成功的列表接口 `data` 直接是数组，**没有分页**（见 §10）。
- **超时与重试（读接口）**：§3 读接口全部是纯读、无副作用，可任意重试。客户端超时建议初值：`resolve` 3 秒；全量列表/change-log 10 秒。500 `database unavailable` 类错误可重试；
- **超时与重试（写接口）**：⚠️ 幂等性逐接口不同，超时后**不要盲目重试**：
  - **可安全重试**：软删除/恢复知识点、撤回答案、启用/停用知识库（天然幂等，重复调用不改变结果；撤回/删除的"原因"以第一次成功写入的为准，重试不覆盖）；
  - **重试会产生重复数据**：写答案、编辑答案、设为默认——每次成功调用都在版本链尾部**追加一个新版本**，超时重试可能落两个内容相同的版本（不报错）。建议超时后先用读接口（`answer-groups` / `change-log`）确认上一次是否已生效，再决定补发；
  - **重试会被业务拦截**：创建知识库/知识点重复时分别报 400 名称/标题已存在，可据此判定首次已成功；
- 建议保留降级兜底：知识库异常时业务按"无知识/无经验"路径继续，不阻塞你方主流程。

---

## 3. 读接口面

| # | 接口 | 用途 |
|---|---|---|
| 3.1 | `GET /knowledge-bases` | 知识库列表（选库/下拉） |
| 3.2 | `GET /dimensions`、`GET /knowledge-bases/{kb_id}/enabled-dimensions` | 维度定义：能用哪些条件查 |
| 3.3 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve` | **单点条件查询（核心接口）** |
| 3.4 | `GET /knowledge-bases/{kb_id}/knowledge-points` | 知识点列表 + 条件筛选（全量拉取） |
| 3.5 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}` | 知识点详情（单条） |
| 3.6 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answer-groups` | 答案分组树（审计/巡检） |
| 3.7 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers` | 知识点全部原始答案版本（含已撤回） |
| 3.8 | `GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/change-log`、`GET /change-log` | 变更留痕 |
| 3.9 | `GET /knowledge-bases/{kb_id}/stats` | 统计（对账用） |
| 3.10 | `GET /knowledge-bases/{kb_id}/dimension-values` | 某维度在本库现存答案里出现过的取值（下拉候选） |

> **维度对子服务只读**：3.2 的两个 GET 是维度相关的全部开放接口。维度的创建、修改、启用/停用属于运营侧管理动作，不对机器调用开放（见 §5）。

### 3.1 知识库列表

```
GET /knowledge-bases?status=active
```

`status` 可选 `active`/`deprecated`，不传返回全部；**建议固定传 `active`**。响应 `data` 为数组，每项：

```json
{
  "id": 2,
  "name": "全局打标经验",
  "description": null,
  "status": "active",
  "active_knowledge_point_count": 86,
  "category_id": 5,
  "category_name": "打标类",
  "created_at": "2026-08-01T10:00:00",
  "updated_at": "2026-08-05T09:00:00"
}
```

`category_id`/`category_name` 为所属分类（两者皆 `null` = 未分类）；分类树本身的管理不对子服务开放。

注意：`kb_id` 是环境相关的自增整数，测试与生产必然不同，请**按环境分别配置**、不要写死跨环境复用。当前生产业务库为 kb=2（全局打标经验）/ kb=3（打标助手规则库）/ kb=4（操作助手规则库）；kb=1 为我方内部调试库，**请勿对接**（将清理/停用）。

### 3.2 维度：先确认能查什么条件

```
GET /dimensions                                        （全局维度定义）
GET /knowledge-bases/{kb_id}/enabled-dimensions        （该库启用的维度）
```

两者结构相同，均只返回全局 `active` 的维度：

```json
[ { "key": "tenant", "label": "tenant", "field_type": "text", "weight": 60 } ]
```

查询/写入某库前**先调后者**确认该库可用哪些维度——条件组合 `coord` 里出现该库未启用的 key 会被 400 拒绝，不做静默忽略。`key` 即展示名，创建后不可变；`field_type` 为 `text`/`number`/`date`/`boolean`。

### 3.3 单点条件查询 resolve（核心接口）

已知知识点、想问"当前条件下答案是什么"时直接用它，不需要先拉列表再自己筛：

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/resolve?at=2026-08-18&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `at` | 今天（服务端时区，UTC+8） | 查询时间点，`YYYY-MM-DD` |
| `coord` | `{}` | 条件组合，JSON 对象 URL-encode 后放查询串，如 `coord={"tenant":"A","客户等级":"VIP"}` |

响应 `data`：

```json
{
  "status": "exact",
  "answer": {
    "id": 55,
    "knowledge_base_id": 2,
    "knowledge_point_id": 12,
    "coord": { "tenant": "A" },
    "content": "……答案全文……",
    "effective_time": "2026-08-01",
    "note": null,
    "revoked": false,
    "operator": "张三",
    "source": "manual",
    "created_at": "2026-08-01T10:00:00"
  }
}
```

- `status` 五种取值：`exact`（条件精确命中）/ `weighted`（按权重回退命中）/ `default`（空条件查询命中默认答案）/ `fallback-latest`（空条件且无默认答案时兜底取最新，**带非空条件查询不会出现**）/ `none`（无兼容答案，`answer` 为 `null`）。除 `none` 外都表示"有答案"，区别只是命中方式；
- 知识点不存在 → 404 `知识点不存在`；知识点已被软删除 → **200** + `{"status":"none","answer":null}`（视为无答案，不是错误）；
- `content` 是**全文**，无长度上限、无摘要截断。

### 3.4 知识点列表 + 条件筛选（全量拉取）

```
GET /knowledge-bases/{kb_id}/knowledge-points?status=active&keyword=退货&at=2026-08-18&coord={"tenant":"A"}
```

| 参数 | 默认 | 说明 |
|---|---|---|
| `status` | `active` | `active` / `deleted` |
| `keyword` | 无 | **仅标题**模糊匹配（大小写不敏感，≤255 字符）；不支持按内容/备注搜索 |
| `at` / `coord` | 同 3.3 | 不传 `coord` 时返回全部匹配知识点，各自带解析预览；**传了非空 `coord` 后，解析结果为 `none` 的知识点整条排除**，不出现在列表里 |

每项在知识点字段基础上多带 `resolved` 字段（结构与 3.3 的响应完全相同，`answer.content` 同样是全文）——**按条件全量拉取一次列表调用即可拿到全部生效答案含全文，不存在 N+1**。

### 3.5 知识点详情

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}
```

返回单个知识点对象（字段同 §4.2 创建接口的响应），含软删除状态与留痕（`status`/`deleted_at`/`delete_reason`）、`active_answer_count`。写操作前核对状态用。

### 3.6 答案分组树（审计/巡检用）

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answer-groups?at=2026-08-18
```

返回该知识点**全部**条件组合分组（含已撤回的，标 `revoked: true`），每组带 `version_count`、`latest_answer`、`live_answer`（`at` 时点无生效版本则为 `null`）。展示"这个知识点历史上配置过哪些条件"用，不做筛选、不参与解析。**写答案超时后判定是否需要重发，优先用它**（§2）。

### 3.7 知识点全部原始答案版本

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers
```

返回该知识点的**每一个答案版本**（所有条件组合、含已撤回、含被新版取代的历史版本），不做解析、不做过滤。数据核对/全量同步用；日常查询请用 resolve（3.3）。

### 3.8 变更留痕

```
GET /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/change-log    （单知识点）
GET /change-log                                                     （全局，跨库）
```

写操作时间线（创建/编辑/撤回/恢复），每条带 `before_content`/`after_content`；全局版每条额外带 `knowledge_base_id`/`knowledge_base_name`/`knowledge_point_title`。只读审计视图，不受知识点软删除/知识库停用影响。行内 `status`：`live`（链当前生效版本）/ `superseded`（被更晚版本取代）/ `revoked`（链已撤回）。

**全局 `/change-log` 无分页、全量返回**——核对场景优先用单知识点接口定点查，全局接口仅审计用并自行截断。

### 3.9 统计

```
GET /knowledge-bases/{kb_id}/stats
```

`{ "subject_count", "active_answer_count", "enabled_dimension_count", "today_change_count" }`，只统计 `active` 知识点范围。用于数据交接对账（条数核对 + 抽样 resolve 比对全文）。

### 3.10 维度取值候选

```
GET /knowledge-bases/{kb_id}/dimension-values?dimension_key=tenant
```

返回该维度在本库**现存**答案条件里出现过的全部取值（去重排序的数组）。"现存" = 未撤回且知识点未删，与 resolve 的可命中范围一致。`dimension_key` 未在本库启用 → 400 `维度 X 未在本知识库启用`。

---

## 4. 写接口面

### 4.0 写入通用规则（先读）

- **必须携带有效 `X-Service-Token`**（§1.1），且你方白名单需已开通写接口面；
- 请求体一律 JSON（`Content-Type: application/json`）；
- **操作人留痕（强制）**：写请求必须携带 `X-Service-Operator` 请求头（§1.2 第 3 条），值为你方系统内**实际发起本次操作的人**的账号或姓名（自动任务写任务标识，如 `auto-sync-job`）。落库的 `operator` 记为 `<你方系统编码>:<操作人>`（如 `yhfkglxt:zhangsan`），在变更留痕、运营页面中以此显示。缺此头的写请求直接 400。该值由你方自报、知识库侧不校验真伪——追责链路 = 我方留痕定位到你方系统与操作人 + 你方登录日志；
- **操作系统留痕（自动，无需你方做任何事）**：每条写入自动记录 `source_system` = 你方系统编码（取自服务 Token 的平台校验结果，**不可自报、不可伪造**）。运营端写入记为 `tyzsk`（页面显示"本系统"）；
- **数据来源自报**：写答案类接口（4.2 的 `default_answer`、4.8、4.9）请求体支持可选字段 `source`，取值 `人工填报` / `AI生成`（其他值 422），**缺省 `人工填报`**——内容是 AI 生成还是人工填写只有你方知道，请如实自报。第三种取值 `批量导入` 由批量导入端点（4.3）服务端固定写入，不接受自报；编辑（4.9）不传 = 继承被编辑答案的值，显式传值可改写（如 AI 内容人工核对后改报 `人工填报`）；设为默认（4.10）随源答案，不接受自报；
- **知识库停用（`deprecated`）不拦截写入**——停用只影响列表默认过滤，不冻结数据。写入前请自行确认目标库状态符合你方预期；
- **软删除的知识点是只读的**：对已删除知识点写答案/编辑答案/改标题一律 400（`知识点已删除，无法…`）；仅"恢复"（4.7）与"撤回答案"（4.11）例外；
- 幂等性与重试策略见 §2；
- 分类树（`/categories`）与维度定义的写接口不开放（§5）——创建/修改知识库时 `category_id` 只能挂到**已存在**的分类上，需要新分类请走运营渠道。

### 4.1 知识库

> 本系统**没有删除知识库的接口**（物理删除不存在），生命周期终态是"停用"。以下即知识库的全部管理动作，均开放。

#### 创建知识库

```
POST /knowledge-bases
{
  "name": "售后规则库",                          // 必填，≤255，全局唯一（大小写/重音不敏感去重）
  "description": "……",                          // 可选，≤2000
  "enabled_dimension_keys": ["tenant", "地区"],  // 可选，创建时直接启用维度（省去二段式操作）
  "category_id": 5                              // 可选，null/省略 = 未分类；分类必须已存在
}
```

响应为知识库对象（同 3.1 单项）。错误：400 `知识库名称已存在，请使用其他名称`；400 `维度「X」不存在` / `维度「X」已停用，无法启用`；404 `分类不存在`。

#### 修改知识库（名称/描述/挂分类）

```
PATCH /knowledge-bases/{kb_id}
{ "name": "…", "description": "…", "category_id": 7 }
```

三个字段都可选，**未传的字段不改**；`description`/`category_id` 传显式 `null` 分别表示清空描述 / 置为未分类（与"未传"含义不同）。

#### 启用 / 停用

```
POST /knowledge-bases/{kb_id}/activate
POST /knowledge-bases/{kb_id}/deactivate
```

无请求体，幂等。停用后库与数据完整保留，读接口仍可访问（列表建议按 `status=active` 过滤）。

#### 配置本库启用的维度

```
PUT /knowledge-bases/{kb_id}/enabled-dimensions
{ "dimension_keys": ["tenant", "地区"] }
```

**整组替换**（不是增量开关）：传 `[]` 表示清空全部启用维度——请先 GET 当前列表、在其基础上增删后整组提交。只能从全局 `active` 的维度中选（400 `维度「X」不存在` / `已停用`）。响应为替换后的启用维度列表（同 3.2）。

> 注意：这是"从全局维度字典里为本库勾选子集"，**不是**创建/修改维度本身——维度定义仍然只读（§3.2）。

### 4.2 知识点：创建

```
POST /knowledge-bases/{kb_id}/knowledge-points
{
  "title": "退货政策",                 // 必填，≤255，同库内唯一
  "default_answer": {                  // 可选：创建同时写入默认答案（coord={} 链的首个版本）
    "content": "……",                   // 必填，全文，无长度上限
    "effective_time": "2026-08-20",    // 必填，YYYY-MM-DD
    "note": "初始导入",                 // 可选
    "source": "人工填报"                // 可选：人工填报（默认）/ AI生成（§4.0 数据来源自报）
  }
}
```

响应为知识点对象：

```json
{
  "id": 12, "knowledge_base_id": 2, "title": "退货政策", "status": "active",
  "operator": "yhfkglxt:zhangsan", "active_answer_count": 1,
  "created_at": "…", "updated_at": "…", "deleted_at": null, "delete_reason": null
}
```

错误：400 `该知识库下已存在同名知识点`（标题库内唯一，大小写不敏感）。

### 4.3 知识点：批量导入

```
POST /knowledge-bases/{kb_id}/knowledge-points/batch-import
{ "items": [ { "title": "…", "default_answer": {…} }, … ] }    // 1~500 条/批
```

**部分成功、逐项报告**——个别条目失败（如标题重复）不影响同批其他条目：

```json
{
  "created_count": 8, "failed_count": 2,
  "results": [
    { "index": 0, "status": "created", "title": "…", "knowledge_point_id": 101 },
    { "index": 1, "status": "failed",  "title": "…", "reason": "该知识库下已存在同名知识点" }
  ]
}
```

超过 500 条请分批；批内标题彼此重复的，先到先得。

### 4.4 知识点：列表 / 详情（查）

即 §3.4 / §3.5，读写共用同一套接口。

### 4.5 知识点：改标题

```
PATCH /knowledge-bases/{kb_id}/knowledge-points/{kp_id}
{ "title": "新标题" }
```

只能改标题（知识点没有其他自有字段；内容都在答案上）。已删除的知识点 → 400 `知识点已删除，无法编辑标题`。

### 4.6 知识点：删除（软删除）

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/delete
{ "delete_reason": "业务下线" }        // 必填，≤500
```

逻辑删除：知识点从 `active` 列表与 resolve 中消失（resolve 返回 `{"status":"none"}`），数据与留痕完整保留，**kp_id 永不复用**。幂等——对已删除的知识点重复调用返回成功，且**不覆盖**首次记录的删除原因。

### 4.7 知识点：恢复

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/restore
```

无请求体，幂等。恢复后 kp_id、全部答案原样回来；历史的 `deleted_at`/`delete_reason` 保留作留痕。

### 4.8 答案：写入（新增一个版本）

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers
{
  "content": "……",                     // 必填，全文
  "effective_time": "2026-08-20",      // 必填，YYYY-MM-DD；当天生效即时可查
  "coord": { "tenant": "A" },          // 可选，默认 {}（默认答案链）
  "note": "…",                         // 可选，版本备注
  "source": "AI生成",                  // 可选：人工填报（默认）/ AI生成（§4.0 数据来源自报）
  "reactivate_reason": "…"             // 目标链当前是撤回态时必填（≤500），其余情况忽略
}
```

- `coord` 里的维度必须**已在本库启用**，取值按维度 `field_type` 严格校验（与查询同一套规则，见 §7 表）；
- **同一 coord = 在该版本链尾部追加新版本**，历史版本不受影响；新 coord = 开一条新链。两者对调用方是同一个动作，服务端按 coord 自动归链；
- 目标链此前被撤回的：带上 `reactivate_reason` 会**恢复整条链**并追加新版本（撤回不是终态）；不带则 400 `该条件组合此前已被撤回，重新启用需填写原因`；
- 知识点已删除 → 400 `知识点已删除，无法写入答案`。

响应为新答案版本对象（字段见 §9）。

### 4.9 答案：编辑（追加新版本 / 迁移条件）

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers/{answer_id}/edit
{
  "content": "……",                     // 必填
  "effective_time": "2026-08-20",      // 必填
  "note": "…",                         // 可选
  "coord": { "tenant": "B" },          // ⚠️ 可选，语义见下——不传 ≠ 传 {}
  "source": "人工填报",                // 可选：不传 = 继承被编辑答案的数据来源；传值可改写
  "migration_reason": "…",             // 改变条件组合时必填（≤500）
  "reactivate_reason": "…"             // 编辑落点链是撤回态时必填（≤500）
}
```

"编辑"**不修改原答案行**，而是产生新版本（响应里 `id` 是新的）：

- **`coord` 不传（字段整个省略）**：条件不变，在 `answer_id` 所在链的尾部追加新版本；
- **`coord` 传了且与原条件不同**：**迁移**——原条件的整条链被整体撤回（`migration_reason` 必填并记为撤回原因），新条件链追加/新建版本。400 `变更适用条件需要填写迁移原因`；
- **`coord` 传 `{}`**：改为默认条件（也是一种迁移）；**`coord` 传 `null` 会被 400 拒绝**（`coord 不能为 null；不携带该字段表示条件不变，传 {} 表示改为默认条件`）；
- 落点链是撤回态时同 4.8：`reactivate_reason` 必填。

### 4.10 答案：设为默认

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers/{answer_id}/promote-to-default
{ "effective_time": "2026-08-20", "note": "…", "reactivate_reason": "…" }
```

把 `answer_id` 那条答案的**内容**复制成默认链（`coord={}`）的新版本；源答案本身不受任何影响。默认链是撤回态时 `reactivate_reason` 必填。

### 4.11 答案：撤回（整链逻辑删除）

```
POST /knowledge-bases/{kb_id}/knowledge-points/{kp_id}/answers/{answer_id}/revoke
{ "revoke_reason": "内容过时" }        // 必填，≤500
```

- 撤回的对象是 `answer_id` **所在的整条版本链**（该条件组合下所有版本），不是单个版本——链上任意一个 `answer_id` 效果相同；
- 撤回后该条件下 resolve 不再返回结果；历史与留痕永久可查（answer-groups 里标 `revoked: true`）；
- 幂等：重复撤回返回成功，首次记录的原因不被覆盖；
- **撤回不是终态**：之后往该链写入新版本（4.8/4.9，带 `reactivate_reason`）会恢复整条链；
- 知识点已软删除时**仍可撤回**其答案（软删与撤回是两个独立维度）。

---

## 5. 不对子服务开放的接口（调用返回 403）

| 类别 | 说明 |
|---|---|
| **维度写接口** | 维度的创建、修改（label/权重/默认值）、启用/停用，以及 `/admin/dimensions` 管理视图——维度是全局字典，改动影响所有库的查询语义，仅运营侧 admin 操作。需要新维度请走运营渠道提交 |
| **知识库分类树** | `/categories` 全部接口（建/改/删/移动）。知识库挂分类用 §4.1 的 `category_id`；列表接口已随知识库带回分类信息，无需拉树 |
| **答案关联** | 关联列表/AI 分析/关联任务等运营侧能力，暂不对机器调用开放 |
| **用户与鉴权管理** | `/users`、`/auth/*`（机器调用不涉及） |

---

## 6. 核心概念

| 概念 | 说明 |
|---|---|
| 知识库 | 顶层容器，`status` 为 `active`/`deprecated`（无物理删除）。知识点、答案归属唯一知识库；**不支持跨库检索** |
| 维度 | 全局定义的条件字段（如 `tenant`、`地区`）。`key`（即展示名，不可改）、`field_type`、`weight`（1~100，权重回退用）。**对子服务只读** |
| 知识库启用维度 | 每库从全局维度勾选子集；只有本库启用且全局 `active` 的维度能出现在条件组合里（读写同一约束） |
| 知识点 | 可被问到的"问题"，同库内 `title` 唯一；`status` 为 `active`/`deleted`（软删除，可恢复，**kp_id 永不复用、恢复后不变**） |
| 答案 | 知识点在某条件组合下的具体说法：`content`、`effective_time`、`coord`、`revoked`。**不同 `coord` 的答案彼此独立并存**，互不覆盖 |
| 条件组合 coord | `{维度key: 取值}` 字典；`{}` 即默认答案。相同 coord 的答案构成一条**版本链** |
| 版本链 | 编辑 = 链尾追加新版本，历史版本永久只读保留，无物理删除 |
| 撤回 | 对整条版本链的逻辑删除（原因必填）；撤回后该条件下查不到结果。**撤回不是终态**：链上写入新版本（需 `reactivate_reason`）会恢复整条链 |
| 解析 resolve | 给定"库 + 知识点 + 时间 + 条件"，按 §7 算法算出返回哪条答案 |

---

## 7. 解析算法（`status` 怎么来的）

对一个知识点，在给定 `at` + 查询条件 `Q` 下：

1. 只考虑 `effective_time <= at` 且未撤回的答案版本；同一 `coord` 内按 `(effective_time, created_at, id)` 取最大者为该组当前版本（同日多次编辑，最后一次胜出）；
2. **`Q` 为空**：有 `coord={}` 默认组 → `default`；否则取全部组里最新一条 → `fallback-latest`；
3. **`Q` 非空**：筛"条件兼容"的组——该组自己写明的每个维度取值都必须与 `Q` 一致；`Q` 问到但组没写的维度不参与比较（因此 **`coord={}` 组永远兼容**，充当最低优先级候选）：
   - 无兼容候选 → `none`；
   - 有候选：按 `(涉及维度数, 涉及维度权重和, effective_time, created_at, id)` 取最大；
   - 结果条件与 `Q` 完全一致 → `exact`；否则 → `weighted`（权重回退命中，展示/使用时务必与精确命中区分）。

条件之间固定"且"关系，**不支持"或"**；数值/日期维度只做精确相等匹配，不支持区间/大于/小于。

`coord` 取值按维度 `field_type` 严格校验（**写入与查询同一套规则**）：

| `field_type` | 允许取值 | 说明 |
|---|---|---|
| `text` | 字符串 | 会 strip；去空格后为空串等价于"没写这个条件" |
| `number` | JSON 数字或数字字符串 | 超过 2^53 的整数请以字符串传入保留精度；超精度浮点直接 400 |
| `date` | `"YYYY-MM-DD"` | 必须是合法 ISO 日期，不接受带时间 |
| `boolean` | JSON `true`/`false` | **严格类型**，字符串 `"true"` 会被拒绝 |

---

## 8. 租户（tenant）约定

- 租户隔离用全局 `tenant` 维度（text 型）表达：单租户知识 `coord={"tenant":"<租户标识>"}`，全局共享知识 `coord={}`；
- **强烈建议子服务所有读取强制携带非空 `tenant` 条件**。这样：
  - 只会命中两类答案——同 `tenant` 取值的答案（`exact`/`weighted`），以及全局共享白名单内 `coord={}` 的答案（`weighted`）；**不会命中其他租户的答案**；
  - `fallback-latest` 状态被完全规避（它只在空条件查询时出现）；
- **写入侧同样强制**：子服务写入的租户级知识请一律带 `coord={"tenant":"…"}`——系统**没有"维度必填"校验**，漏写 `tenant` 的答案对所有租户查询都是兼容候选。此风险由录入规范 + 每周 tenant 巡检兜底；哪些知识点允许 `coord={}`（全局共享白名单）由双方确认后备案。

---

## 9. 答案对象字段承诺

resolve、列表 `resolved`、分组树、写接口响应中的答案对象为同一结构，固定携带以下字段，**只增不删**；读接口字段与行为变更提前通知：

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | int | 答案版本 ID；编辑=追加新版本，`id` 随之变化 |
| `knowledge_base_id` / `knowledge_point_id` | int | 必选；引用锚点建议用 `knowledge_point_id` + `coord`（kp_id 永不复用） |
| `coord` / `coord_hash` | object / string | 条件组合及其规范化哈希 |
| `content` | string | **全文**，无长度上限 |
| `effective_time` | date | 生效日期（日期粒度，闭区间 `<= at` 即生效） |
| `note` | string\|null | 版本级备注 |
| `revoked` / `revoked_at` / `revoked_by` / `revoke_reason` | — | 撤回状态与留痕 |
| `reactivated_at` / `reactivated_by` / `reactivate_reason` | — | 最近一次恢复的留痕（恢复时 `revoked_*` 保留原样当历史；只保留最近一轮撤回/恢复） |
| `operator` / `created_at` | — | 操作留痕（`operator` 为操作人真实姓名；机器写入为 `<系统编码>:<X-Service-Operator 头值>`，未带头的历史机器数据为 `<系统编码>-service`；2026-08-14 前的历史数据固定 `admin`） |
| `source` | string | **数据来源（产生方式）**：`人工填报` / `AI生成` / `批量导入` 三值（2026-08-21 前的历史值"人工编辑"已归一为"人工填报"）。写入时自报规则见 §4.0 |
| `source_system` | string | **操作系统（写入方系统编码）**：服务凭证写入 = 你方系统编码（自动记录，不可自报），运营端写入 = `tyzsk`。变更留痕接口（3.8）逐条同样携带本字段与 `source` |

时间口径：`created_at` 等为不带时区的 naive datetime，服务端时区 UTC+8；`effective_time`/`at`/日期型维度取值为纯日期 `"YYYY-MM-DD"`。"当天录入、`effective_time` 填当天"即时生效；变更**无主动通知/回调**，请以你方缓存 TTL 收敛（建议分钟级）。

---

## 10. 错误表（业务部分）

鉴权类错误见 §1.4。对接面实际可能遇到的业务错误：

| HTTP | `msg` | 触发场景 |
|---|---|---|
| 400 | `维度 X 未在本知识库启用` | 读/写的 `coord` 用了该库未启用的维度 key |
| 400 | `维度 X 取值类型错误，应为...` / `...精度超出...范围` | `coord` 取值类型/精度不满足维度 `field_type`（§7 表） |
| 400 | `coord 参数必须是一个 JSON 对象` | 查询串 `coord` 解析后不是对象（如传了数组） |
| 400 | `知识库名称已存在，请使用其他名称` | 建库/改名撞名（大小写不敏感） |
| 400 | `该知识库下已存在同名知识点` | 建知识点/改标题撞名 |
| 400 | `维度「X」不存在` / `维度「X」已停用，无法启用` | 建库带维度 / PUT enabled-dimensions 引用了无效维度 |
| 400 | `知识点已删除，无法编辑标题 / 无法写入答案 / 无法编辑答案` | 对软删除知识点做除恢复/撤回外的写操作 |
| 400 | `变更适用条件需要填写迁移原因` | 编辑答案改了 `coord` 但没带 `migration_reason` |
| 400 | `该条件组合此前已被撤回，重新启用需填写原因` | 往撤回链写版本没带 `reactivate_reason` |
| 400 | `coord 不能为 null；…` | 编辑答案时 `coord` 传了字面 `null` |
| 404 | `知识库不存在` / `知识点不存在` / `答案不存在` / `分类不存在` | 路径/引用 ID 找不到资源 |
| 422 | `请求参数校验失败` | 请求体/查询参数格式不对（缺必填字段、超长等）；字段级细节只记服务端日志，不回传 |
| 500 | `内部错误` / `database unavailable` | 服务端异常/数据库不可用，读接口可重试，写接口按 §2 幂等性判断 |

---

## 11. 已知限制（对接前确认可接受）

| 限制 | 说明 | 建议 |
|---|---|---|
| 无分页 | 列表/维度/分组树/change-log 全量返回 | 按"全量拉取 + 本地缓存"设计 |
| 无增量、无回调 | 无按时间过滤、无变更推送 | 缓存 TTL 自然收敛；核对用单知识点 change-log |
| 无跨库检索 | 所有查询限单个 `kb_id` | 按库分别拉取 |
| 无"或"条件、无区间匹配 | 条件固定"且"，数值/日期只做相等 | 复杂筛选在你方本地做 |
| 无限流、无 SLA 机制 | 服务端当前不限流 | 你方自行控并发 + 保留降级兜底 |
| keyword 仅搜标题 | 不支持内容/备注搜索 | 需要全文检索请在你方本地建索引 |
| 写答案无防重 | 同链重复提交相同内容会落两个版本，不报错 | 超时重试前先查 answer-groups 确认（§2） |
| 无维度必填校验 | 漏写 `tenant` 的答案对所有租户可见 | 写入侧自律 + 每周巡检兜底（§8） |
| 撤回留痕粒度 | 只保留最近一轮撤回/恢复，更早轮次不可追溯 | 完整事件史规划在 V2 |

---

## 12. 接入流程（新子服务 checklist）

1. **系统登记**：确认你方系统已在统一身份平台登记（有系统编码 + 客户端密钥）；未登记先走平台流程；
2. **知识库侧登记**：将你方**系统编码**、用途、**所需接口面（只读 / 读写）**告知知识库方，我方将你方加入来源系统白名单（逐条确认可访问面，写接口默认不开）并回复确认；
3. **拿地址**：向我方索取测试/生产环境服务地址；
4. **联调（测试环境）**：
   - 按 §1.2 从平台申请服务 Token（`targetSystem=tyzsk`），带 `X-Service-Token` 调 `GET /knowledge-bases` 验证 200；
   - 调 `GET /knowledge-bases/{kb_id}/enabled-dimensions` 确认可用维度；
   - 按你方业务跑通 resolve / 列表拉取，覆盖 `exact`/`weighted`/`none` 三种状态的处理；
   - **接入写接口的**：在测试库跑通建知识点 → 写答案 → resolve 查回 → 编辑（含迁移）→ 撤回 → 带原因恢复的完整链路；验证 `coord` 校验 400、撤回链重写 400、**漏带 `X-Service-Operator` 400** 三类拦截，并在 change-log 里确认 `operator` 记为 `你方编码:操作人`；
   - 验证 Token 过期重取、401 单次重试、降级兜底逻辑；
5. **kb_id 配置**：按环境分别配置 kb_id（测试/生产不同），并与我方核对库名对应关系；
6. **上线**：与我方确认生产网络边界（内网/白名单）后切生产地址。

联系方式 / 变更通知渠道：【待填：对接群 / 邮件，发布前一个工作日通知】

---

## 附录：变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.3 | 2026-08-21 | 变更留痕三元组：新增 `source_system`（操作系统，服务端自动记录，不可自报）；`source` 收敛为 `人工填报`/`AI生成`/`批量导入` 三值枚举并开放写入侧自报（§4.0/§4.8/§4.9 新增可选 `source` 字段，批量导入固定、设默认随源答案、编辑默认继承）；历史值"人工编辑"归一为"人工填报"；§9 字段表同步 |
| v1.2 | 2026-08-21 | 写操作审计留痕升级：写接口（POST/PATCH/PUT）**强制携带 `X-Service-Operator` 请求头**（操作人，中文 URL 编码，缺失 400），`operator` 落库由 `<系统编码>-service` 改为 `<系统编码>:<操作人>`（§1.2/§4.0/§9）；错误速查表补 400 操作人缺失项；联调 checklist 补对应验证项 |
| v1.1 | 2026-08-20 | 对接面从"纯只读"扩展为读写：新增 §4 写接口面（知识库管理除删除、知识点全套增删改查、答案写入四件套）；读接口面补入知识点详情、全部答案版本、维度取值候选三个 GET；明确维度对子服务只读；写接口强制服务 Token、不进免鉴权豁免；补写入幂等性/重试指引与机器写入 operator 留痕规则 |
| v1.0 | 2026-08-18 | 首版（只读接口面） |
