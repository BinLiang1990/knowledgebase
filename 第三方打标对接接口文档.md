# 企业打标服务 · 对接接口文档

面向对接方（企业系统）。你把企业数据送过来，我们异步打标，你再按游标把结果取回去；
需要修正标签时，可以直接把新增与编辑写回来。

**只有三个接口**：

| 接口 | 用途 |
|---|---|
| `POST /api/partner/enterprises` | 上送企业数据（最后一片自动提交） |
| `GET /api/partner/tasks/{task_no}/results` | 拉进度与结果（游标即确认） |
| `POST /api/partner/tags` | 新增 / 编辑标签（可顺带建企业关联） |

请求全部由你方发起，我们不会回调你方任何地址，也不需要你开放任何入站端口。

---

## 1. 租户：你的数据边界

你的数据放在**你自己的租户**里。租户由你用 `tenant_code` 指定，**首次出现即自动创建**，
之后一直复用。

- `tenant_code` 是**你自己的**租户标识（例如 `hz-branch`、`group-a`），我方建立映射后，
  在响应里把我方的真实租户 ID 回给你备查；
- 各对接方的 `tenant_code` **互相独立**：你的 `default` 和别家的 `default` 是两个不同的租户，
  不会串；
- **你只能访问自己创建的租户**。传别的（包括我方真实存在的租户 ID）一律返回 403；
- 不传 `tenant_code` 时，落到我方为你预设的**兜底租户**，`tenant_code` 为 `default`。

### 命名规则

| 规则 | 说明 |
|---|---|
| 字符集 | 字母、数字、下划线、短横线，最长 128 位 |
| **不能是纯数字** | `"3"`、`"1001"` 会被拒。这类值多半是误把我方租户 ID 填了进来，直接建租户会造出你并不想要的数据 |
| 建议 | 用有含义的短标识，如 `hz-branch`、`group-a`、`prod` |

### 可创建租户数有上限

默认 10 个，可按需调整——需要更多时联系我方提额。超额时返回：

```json
{ "detail": "可创建租户数已达上限（10 个），请联系我方提额",
  "code": "TENANT_QUOTA_EXCEEDED", "quota": 10 }
```

**只有上送接口能创建租户**，拉结果和写标签都不会顺手开新租户，传了不存在的 `tenant_code`
一律 403。

---

## 2. 任务状态机

一次业务提交对应一个任务，状态只有四个：

```
uploading  ──收到 is_last=true 那片──►  running  ──全部企业跑完──►  done
（上送中）                              （打标中）                   （已完成）
                                            │
                                            └──一条结果都没产出──►  failed
```

- `uploading`：还在收你的切片，此时可以补传、可以覆盖；
- `running`：已提交并排队打标，此时**不再接受上送**；
- `done`：所有企业都有了下落（成功或失败），结果可全部取回；
- `failed`：异常情况（例如整批被我方中止），一条结果都没有。

状态只在你调用接口时返回，不需要你单独查。

---

## 3. 鉴权

我们为你分配一对凭证：`app_id`（可公开）与 `app_secret`（务必保密，只在创建和轮换时下发一次）。

每个请求带四个头：

| 请求头 | 说明 |
|---|---|
| `X-Partner-App-Id` | 分配给你的 app_id |
| `X-Partner-Timestamp` | Unix 秒级时间戳 |
| `X-Partner-Nonce` | 随机串，5 分钟内不可重复 |
| `X-Partner-Signature` | 签名，十六进制小写 |

签名串用 `\n` 连接五段：

```
METHOD \n PATH \n TIMESTAMP \n NONCE \n SHA256(BODY)
```

- `PATH` **带查询串**，逐字取自请求 URL，例如
  `/api/partner/tasks/PT001/results?cursor=0&limit=50`；
- `SHA256(BODY)` 是请求体原始字节的 SHA-256 十六进制小写；GET 无请求体时取空串的摘要
  （即 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`）；
- `signature = HMAC-SHA256(app_secret, 签名串)`。

Python 示例：

```python
import hashlib, hmac, json, time, uuid

def sign(secret, method, path, body: bytes):
    ts, nonce = str(int(time.time())), uuid.uuid4().hex
    raw = "\n".join([method, path, ts, nonce, hashlib.sha256(body).hexdigest()])
    return {
        "X-Partner-App-Id": APP_ID,
        "X-Partner-Timestamp": ts,
        "X-Partner-Nonce": nonce,
        "X-Partner-Signature": hmac.new(secret.encode(), raw.encode(),
                                        hashlib.sha256).hexdigest(),
    }
```

时间戳允许 ±5 分钟偏差，请确保服务器时间同步。

---

## 4. 接口一：上送企业数据

```
POST /api/partner/enterprises
```

一次调用送一个切片。**首片自动建任务，`is_last=true` 那片收完即自动提交并开始打标。**

### 请求

```json
{
  "tenant_code": "hz-branch",
  "tenant_name": "杭州分公司",
  "external_task_id": "ENT-2026-0806-001",
  "task_name": "8月存量企业补标",
  "chunk_no": 1,
  "is_last": false,
  "total": 1200,
  "remark": "8月存量补标",
  "sha256": "3f2a…",
  "enterprises": [
    {
      "external_id": "E10001",
      "name": "杭州某某科技有限公司",
      "uscc": "91330105MA2CD1234X",
      "base_info": {
        "business_range": "工业机器人集成、智能装备研发",
        "registered_capital": "1000万元人民币",
        "establishment_date": "2015-03-12",
        "enterprise_size": "中型"
      }
    }
  ]
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `tenant_code` | 建议必填 | 数据落在哪个租户，见第 1 节。**首次出现即自动创建**。不传则落到兜底租户 |
| `tenant_name` | 否 | 租户显示名，仅在首次创建时使用，方便双方在页面上认出来 |
| `external_task_id` | 是 | **你方的任务号，兼作幂等键**。同一个值只会建出一个任务 |
| `task_name` | 否 | 任务名称，便于双方在页面上回溯这批数据。首片没给、后面某片补上也认 |
| `chunk_no` | 是 | 切片编号，从 1 开始，由你方自行编排 |
| `is_last` | 否 | 默认 `false`。传 `true` 表示这是最后一片，收完即提交 |
| `total` | 否 | 声明的企业总数，仅用于双方核对 |
| `remark` | 否 | 备注，会展示在我方后台 |
| `sha256` | 是 | 本片内容摘要，见下方口径 |
| `enterprises` | 是 | 企业数组，**单片最多 500 条** |

### 切片摘要口径

把 `enterprises` 归一成只含四个键的数组——`external_id`、`name`、`uscc`、
`base_info`（缺失用 `{}`），然后：

```python
raw = json.dumps(payload, ensure_ascii=False, sort_keys=True, separators=(",", ":"))
sha256 = hashlib.sha256(raw.encode("utf-8")).hexdigest()
```

对不上返回 400。联调时对一条样例即可，之后不会再错。

### 响应

```json
{
  "task_no": "PT20260806A7F3",
  "external_task_id": "ENT-2026-0806-001",
  "task_name": "8月存量企业补标",
  "tenant": { "code": "hz-branch", "id": "12",
              "name": "企业管理平台 / 杭州分公司", "created": true },
  "status": "uploading",
  "chunk_no": 1,
  "accepted": 500,
  "duplicated": false,
  "declared_total": 1200,
  "received_total": 500,
  "received_chunks": [1],
  "enterprise_total": 0,
  "stats": { "total": 0, "done": 0, "error": 0, "running": 0, "pending": 0, "cancelled": 0 },
  "max_cursor": 0,
  "acked_cursor": 0
}
```

`is_last=true` 的那次调用会多返回提交结果：

```json
{
  "status": "running",
  "enterprise_total": 1200,
  "new_enterprises": 830,
  "reused_enterprises": 370,
  "job_count": 3,
  "experience_count": 86
}
```

### 补传与覆盖

- **查缺片不需要单独接口**：每次响应都带 `received_chunks`，与你自己的切片计划一比就知道缺哪片；
- **重传同一片**：内容一致返回 `duplicated: true`，不会重复入库；
- **修正数据**：提交前重传同一 `chunk_no`、内容不同，会整片覆盖旧数据；
- **提交后**再上送一律返回 409。数据要改就用新的 `external_task_id` 建新任务。

### 企业字段

`external_id`、`name`、`uscc` 三个必填，缺任一整片拒收。
**`uscc` 必填**是因为同名企业很多，没有它我们无法消歧，联网检索会串到别家企业。

`base_info` 用你们自己的英文列名即可，我们会自动归位。**以下 28 个字段会被送进模型**，
其余字段我们照单留档但不影响标签产出：

```
unified_credit_code  ownership_type       enterprise_type      business_status
establishment_date   legal_representative registered_capital   paid_up_capital
national_industry    industry_category    industry_large_category
industry_middle_category                  enterprise_size      insured_persons_num
province  city  district  street          registered_address   business_address
brand                name_used_before     listing_status       development_stage
technology_track     business_range       enterprise_introduction  summary_content
```

其中 `business_range`（经营范围）信息量最大，直接决定标签质量，强烈建议提供。
`business_range`、`enterprise_introduction`、`summary_content` 各**只取前 400 字**。

**三个值转换请在你方完成**：

| 字段 | 请传 |
|---|---|
| `establishment_date` | `"2015-03-12"`，不要带时分秒 |
| `enterprise_size` | `"微型"` / `"小型"` / `"中型"` / `"大型"`，不要传 1/2/3/4 |
| `registered_capital` | 文本形式 `"1000万元人民币"`，不要传纯数值 |

其余字段一律传字符串；**空值请不要传该键**，不要传 `null` 或空串。

**以下字段请不要传**：联系人、电话、邮箱等个人信息（我们不接收，传了也会丢弃）；
你方已有的业务标签与评级（喂给模型会让 AI 抄你们的结论，产出的标签就失去独立价值）。

---

## 5. 接口二：拉取进度与结果

```
GET /api/partner/tasks/{task_no}/results?cursor=0&limit=50
```

这一个接口同时承担三件事：**看进度**、**取结果**、**确认已收**。

| 参数 | 默认 | 说明 |
|---|---|---|
| `cursor` | 0 | 从这个游标之后开始取。**传入即表示这之前的你都收到了** |
| `limit` | 50 | 最多返回多少家，上限 200。**传 0 表示只看进度、不要结果** |
| `tenant_code` | 可选 | `task_no` 已经唯一确定了任务和它的租户，这个参数只是多一道校验；对不上返回 403 |

> 查询参数参与签名，`PATH` 要连 `?` 后面的部分一起签。

### 响应

```json
{
  "task_no": "PT20260806A7F3",
  "external_task_id": "ENT-2026-0806-001",
  "task_name": "8月存量企业补标",
  "status": "running",
  "stats": { "total": 1200, "done": 380, "error": 5, "running": 16,
             "pending": 799, "cancelled": 0 },
  "cursor": 300,
  "next_cursor": 350,
  "has_more": true,
  "max_cursor": 385,
  "acked_cursor": 300,
  "received_total": 1200,
  "enterprise_total": 1200,
  "items": [
    {
      "external_id": "E10001",
      "enterprise_name": "杭州某某科技有限公司",
      "status": "done",
      "finished_at": 1785001234,
      "tags": [
        {
          "tag_id": "88213",
          "name": "工业机器人集成",
          "category": "核心产品",
          "definition": "为汽车零部件产线提供工业机器人集成方案",
          "evidence": "官网产品页列明六轴机器人集成案例（2026-07）",
          "source_url": "https://example.com/products",
          "source_note": "企业官网产品页2026-07",
          "confidence": 0.9,
          "direction": "neutral",
          "sentiment": "正面",
          "source": "web",
          "gradings": [],
          "tagged_at": 1785001200
        }
      ]
    },
    {
      "external_id": "E10002",
      "enterprise_name": "某某贸易有限公司",
      "status": "error",
      "error_code": "TAGGING_FAILED",
      "error_message": "重试 3 次仍失败",
      "finished_at": 1785001300,
      "tags": []
    }
  ]
}
```

### 怎么正确地拉

```
cursor = 0
while True:
    r = GET .../results?cursor={cursor}&limit=50
    处理 r["items"]
    cursor = r["next_cursor"]
    if not r["has_more"] and r["status"] in ("done", "failed"):
        break
    sleep(10)          # 没有新结果时退避，建议不快于 10 秒一次
```

几条必须理解的规则：

1. **只返回已经跑完的企业。** 还在打标的不会出现。
2. **返回 0 条不是错误**，只表示暂时没有新结果。任务可能要跑很久（1 万家企业约 17 小时），
   所以务必边跑边取，不要等 `done` 再一次性拉。
3. **每次返回的条数不固定**，这是正常的——由 `next_cursor` + `has_more` 保证不重不漏。
4. **同一个 cursor 重复拉，返回内容完全一致。** 结果一旦产出就不再变，可以放心重试。
5. **游标即确认**：你传 `cursor=300`，我们就记录你已确认到 300。不确认也能继续拉，
   这个值只用于双方对账。
6. **失败的企业也会出现在结果流里**，带 `error_code`，这样每一条上送都有下落。
7. **只返回本次任务产出的标签**，不含该企业历史上的标签。同一家企业在多次任务里
   陆续产出的标签，请在你方按 `external_id` 自行累积；我方按任务留档，随时可查。

### 标签字段说明

| 字段 | 说明 |
|---|---|
| `name` | 标签名 |
| `category` | 标签主题（如「核心产品」「行业赛道」） |
| `definition` | 该标签在这家企业语境下的定义 |
| `evidence` | 打标依据，说明为什么打这个标签 |
| `source_url` | 依据来源链接。**只会是 `http(s)://` 开头的可打开地址，或空串**，可直接当 URL 用 |
| `source_note` | 依据的文字出处，如「企查查股权穿透/2026年工商数据」「企业库工商注册信息」。**不是链接**，不要拿去请求 |
| `confidence` | 置信度 0~1 |
| `direction` | `neutral` 画像 / `supply` 供给 / `demand` 采购需求 |
| `source` | 来源：`web` 联网检索、`model` 模型判断等 |
| `gradings` | 标签体系路径。**当前版本恒为空数组**，见下方说明 |
| `tagged_at` | 打标时间（Unix 秒） |

> **关于 `gradings`**：当前版本的打标采用自由生成模式，不做标签体系归类，
> 所以这个字段固定返回空数组。字段保留是为了后续接入体系归类时不改契约。

---

## 6. 接口三：新增与编辑标签

```
POST /api/partner/tags
```

拿到结果之后，你方业务人员如果发现「漏打了某个标签」或「这个定义写得不准」，
用这个接口写回来。

### 新增标签

```json
{
  "tenant_code": "hz-branch",
  "action": "create",
  "name": "工业软件国产替代",
  "category": "核心产品",
  "definition": "指以国产工业软件替代进口的业务方向",
  "sentiment": "正面",

  "enterprise_external_id": "E10001",
  "link": {
    "confidence": 0.9,
    "evidence": "2026年报披露该业务线营收占比31%",
    "source_url": "https://example.com/report.pdf",
    "context_definition": "本企业该业务2026年首次超过30%"
  }
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `tenant_code` | 是 | 标签所在租户，见第 1 节。**此接口不会创建租户** |
| `action` | 否 | 默认 `create` |
| `name` | 是 | 标签名 |
| `category` | 是 | **标签主题**，必须是我方已有主题之一。填错会返回全量可选主题供你挑，见下 |
| `definition` | 否 | 标签的通用定义 |
| `sentiment` | 否 | `正面` / `负面` / `中性` / `风险` / `营销`，默认 `中性` |
| `enterprise_external_id` | 否 | 给了就顺带建立这家企业与该标签的关联，用你上送时的 `external_id` |
| `link` | 与上一项同进退 | 关联信息，见下表 |

`link` 的字段：

| 字段 | 必填 | 说明 |
|---|---|---|
| `evidence` | **是** | 打标依据。这是我方全局门禁，缺了直接拒——没有依据的标签事后无法追溯 |
| `confidence` | 否 | 置信度 0~1，默认 0.8 |
| `source_url` | 否 | 依据的原文链接。**只放 `http(s)://` 开头的真链接**；写「企查查股权穿透」这类文字出处会自动落到 `source_note` |
| `context_definition` | 否 | **这家企业**为什么命中该标签。与 `definition` 分属两层，见下方说明 |
| `direction` | 否 | `positive` / `negative` / `neutral`，默认 `neutral` |

**主题填错时的响应**——不用另外查一个接口，报错里直接给你全部可选值：

```json
{ "detail": "新增标签必须给 category（标签主题）",
  "code": "TAG_CATEGORY_REQUIRED",
  "categories": ["核心产品", "企业性质", "行业赛道", "资质认定", "…"] }
```

### 编辑标签

```json
{
  "tenant_code": "hz-branch",
  "action": "update",
  "tag_id": "88213",
  "definition": "工信部认定的专精特新「小巨人」企业，需连续两年主营收入占比超70%",
  "change_reason": "原定义把省级和国家级混为一谈"
}
```

`tag_id` 取自拉结果时返回的标签。**`change_reason` 必填**——这条会进我方变更历史，
日后双方回溯「这个定义什么时候、被谁、为什么改成这样」全靠它。

### 响应

```json
{
  "action": "create",
  "tag_id": "91002",
  "name": "工业软件国产替代",
  "created": true,
  "changed": [],
  "tag_shared_by": 3187,
  "link": {
    "enterprise_id": "35994",
    "enterprise_tag_id": "1209831",
    "created": true
  }
}
```

| 字段 | 说明 |
|---|---|
| `created` | `true` 新建了标签；`false` 命中同名标签并更新了它 |
| `changed` | 本次实际改动的字段，如 `["definition", "sentiment"]`；没改动就是空数组 |
| `tag_shared_by` | **这个标签当前被多少家企业在用**。改通用定义前请看一眼这个数 |
| `link.created` | `true` 新建关联；`false` 关联已存在，按本次内容更新 |

### 三条需要知道的规则

**一、同名标签不会建出第二条。** 我方按归一化后的名称判重：去掉空格、标点、括号并转小写。
「专精特新（国家级）」与「专精特新国家级」视为同一个标签，第二次提交会**直接更新第一条**
而不是新建，同一租户下不会出现同名标签。

判重**不区分标签是谁创建的**——你提交的名称如果与我方打标产出的某个标签同名，
更新的就是那一条。响应里的 `tag_shared_by` 会告诉你它当前被多少家企业在用；
被替换掉的旧定义我方会留档，改错了可以找我们回滚。

**二、标签定义分两层，多数时候你要改的是第二层。**

| | 落在哪 | 影响范围 |
|---|---|---|
| `definition` | 标签的通用定义 | **该租户下所有用到这个标签的企业** |
| `link.context_definition` | 这家企业的情境说明 | 只有这一家 |

「专精特新小巨人是指工信部认定的中小企业」是通用定义；
「本企业 2026 年入选第六批」是这家企业的情境说明。后者请放 `context_definition`。

改通用定义时，响应里的 `tag_shared_by` 会告诉你这一改影响多少家企业；
我方会把被替换掉的旧定义留档，改错了可以找我们回滚。

**三、关联只能建、不能解除。** 本接口不提供「移除某企业的某个标签」的能力。
如果打错了标签需要解除，请联系我方处理。

---

## 7. 错误码

| HTTP | code | 含义 |
|---|---|---|
| 401 | `PARTNER_APP_ID_MISSING` | 缺 `X-Partner-App-Id` 头 |
| 401 | `PARTNER_UNKNOWN` | 凭证不存在 |
| 401 | `PARTNER_SIGNATURE_MISSING` | 缺签名相关头 |
| 401 | `PARTNER_TIMESTAMP_INVALID` | 时间戳超出 ±5 分钟 |
| 401 | `PARTNER_SIGNATURE_INVALID` | 签名不匹配 |
| 401 | `PARTNER_NONCE_REPLAYED` | nonce 重复 |
| 403 | `PARTNER_DISABLED` | 凭证已停用 |
| 403 | `PARTNER_IP_DENIED` | 来源 IP 不在白名单 |
| 429 | `PARTNER_RATE_LIMITED` | 调用过于频繁 |
| 400 | — | 参数或摘要错误，`detail` 里有具体原因 |
| 404 | — | 任务不存在（或不属于你方） |
| 409 | `TASK_ALREADY_COMMITTED` | 任务已提交，不能再上送新数据。**响应体带 `task_no`**，见第 9 节 |
| 503 | — | 计划内维护，按退避重试 |

**租户相关**（第 1 节）：

| HTTP | code | 含义 |
|---|---|---|
| 400 | `TENANT_CODE_INVALID` | `tenant_code` 格式不合法，或是纯数字 |
| 403 | `TENANT_FORBIDDEN` | 该租户不存在或不属于你方 |
| 429 | `TENANT_QUOTA_EXCEEDED` | 可创建租户数已达上限，带 `quota` 字段 |
| 409 | `TASK_TENANT_MISMATCH` | 同一个 `external_task_id` 换了租户续传，带 `task_no` |

**标签接口相关**（第 6 节）：

| HTTP | code | 含义 |
|---|---|---|
| 400 | `TAG_NAME_REQUIRED` | 新增标签没给 `name` |
| 400 | `TAG_CATEGORY_REQUIRED` | 新增标签没给 `category`，**响应带 `categories` 全量可选值** |
| 400 | `TAG_ID_REQUIRED` | 编辑标签没给 `tag_id` |
| 400 | `CHANGE_REASON_REQUIRED` | 编辑标签没给 `change_reason` |
| 400 | `LINK_REQUIRED` | 给了 `enterprise_external_id` 却没给 `link` |
| 404 | `TAG_NOT_FOUND` | 标签不存在或不属于该租户 |
| 404 | `ENTERPRISE_NOT_FOUND` | 该租户下找不到这个 `external_id` 的企业 |

错误响应统一形如：

```json
{ "detail": "签名校验失败", "code": "PARTNER_SIGNATURE_INVALID" }
```

部分错误会多带字段：429 带 `retry_after`（同时有 `Retry-After` 响应头）或 `quota`，
409 带 `task_no`，`TAG_CATEGORY_REQUIRED` 带 `categories`。

---

## 8. 联调步骤建议

1. 我们提供 `app_id` / `app_secret` 与访问地址；
2. **先定 `tenant_code`**：想好你方按什么维度分租户（一个客户一个？一个区域一个？还是就用一个），
   这个值一旦开始用就别改——改了等于换一个新的空租户；
3. 先用 **1 条企业**跑通：上送（`is_last=true`）→ 拉进度 → 拉结果，确认签名与摘要口径一致；
4. 再用 **100 条**验证多切片、补传、覆盖；
5. 标签写回单独验一次：先故意不传 `category` 拿到全量主题列表，再挑一个正式提交；
6. 首轮正式任务建议控制在 **1000 家以内**，跑通后再放开规模。

### 容量参考

| 指标 | 实测 |
|---|---|
| 单家企业打标耗时 | 平均约 100 秒 |
| 每家产出标签数 | 平均 18～19 个 |
| 1000 家预计耗时 | 约 2 小时 |
| 1 万家预计耗时 | 约 17 小时 |

任务规模不设上限，我们内部会自动拆批执行，对你方始终是一个任务号。

---

## 9. 可靠性与幂等

这一节回答"网络出问题时怎么办"。**所有接口都可以安全重试。**

### 上送超时或 5xx 时，切片可能已经落库了吗？

**可能。** 请求已到达我方、处理完成、只是响应在回程丢了——这是分布式调用的常态。
所以我们保证：**用完全相同的报文重传，永远安全**。

| 重传场景 | 我方行为 |
|---|---|
| 同 `external_task_id`（任务已存在） | 不会建出第二个任务，返回原 `task_no` |
| 同 `chunk_no` + 同 `sha256` | 返回 `duplicated: true`，不重复入库、不重复计数 |
| 同 `external_id` 的企业 | 按 `external_id` 复用同一条企业档案，不会重复创建 |
| 提交后重传**完全相同**的最后一片 | **返回原任务视图（含 `task_no`）**，不会重复提交、不会重复拆批 |
| 提交后上送**不同**的数据 | 409 `TASK_ALREADY_COMMITTED`，**响应体里也带 `task_no`** |

### 最后一片的响应丢了怎么办？

这是最需要留意的场景：`is_last=true` 那次调用成功了，但你没收到响应，
手里没有 `task_no`，就拉不到结果。**两条恢复路径，任选其一**：

1. **原样重传最后一片**（推荐）——同 `chunk_no` + 同 `sha256`，返回 200 与原 `task_no`，
   任务状态照旧是 `running`，不会被重复提交；
2. 如果你已经改了数据再传，会拿到 409，但**响应体里同样有 `task_no`**：

```json
{ "detail": "任务已提交（当前状态 running），不能再上送新数据",
  "code": "TASK_ALREADY_COMMITTED",
  "task_no": "PT20260806A7F3" }
```

两条路都能拿回 `task_no`，不存在"任务成功了却永远找不回来"的情况。

### 拉取接口

只读，天然幂等，可无限次重试。已产出的结果不会变化，同一个 `cursor` 永远返回相同内容。

---

## 10. 字段与限制

### 长度与类型

| 项 | 约束 |
|---|---|
| `external_task_id` | 字符串，≤ 255。**纯数字字符串可以**，但请以字符串传，不要传成 JSON 数字 |
| `external_id` | 字符串，≤ 255，同上 |
| `name` | ≤ 512 |
| `uscc` | ≤ 64 |
| `task_name` | ≤ 255 |
| `task_no`（我方返回） | 固定 20 字符，形如 `PT` + 14 位时间 + 4 位十六进制，例 `PT20260806143146C7E3` |
| `tag_id`（我方返回） | **必定返回**，字符串形式的整数，全局唯一且稳定，不只是"同企业内唯一" |
| 时间戳 | `finished_at`、`tagged_at`、`committed_at` 一律 **Unix 秒级整数**，UTC 纪元（与时区无关） |

### 可空性与枚举

| 字段 | 可能为空？ | 取值 |
|---|---|---|
| `name`、`category`、`definition` | 否 | — |
| `evidence` | **可能为空串** | 依据由模型给出，个别标签可能没有 |
| `source_url` | **经常为空串** | 只有依据来自联网检索原文的标签才有；有值时必定是 `http(s)://` 开头的可打开地址 |
| `source_note` | **可能为空串** | 依据的文字出处描述，与 `source_url` 各自独立：可能只有出处没有链接（依据来自企业库工商信息时），也可能两者都有 |
| `confidence` | 否 | 0~1 的浮点，通常两位小数（如 `0.9`、`0.95`） |
| `direction` | 否 | `neutral` / `supply` / `demand` |
| `sentiment` | **可能为空串** | `正面` / `中性` / `负面` / `风险` |
| `source` | 否 | `web`（联网检索）/ `model`（模型判断）/ `match`（词表匹配）等，**允许新增** |
| `gradings` | 否 | 当前版本恒为 `[]` |

### 结果条目的 status

**只有 `done` 和 `error` 两种**，不会出现 `cancelled`。

⚠️ 但有一个边界要知道：如果我方内部中止了任务，被取消的企业**不会进入结果流**——
它们既不是 `done` 也不是 `error`，你会看到 `stats.total` 大于实际取回的条数，
且任务最终变成 `done` 或 `failed`。请以**任务状态进入终态**（`done`/`failed`）
作为"不会再有新结果"的判断依据，而不是"取回条数等于上送条数"。

### error_code

当前只有 `TAGGING_FAILED`（打标重试耗尽）。**请按"允许出现未知值"实现**——
后续可能细分（如模型不可用、企业信息不足等）。未知错误码按"该企业本次失败"处理即可。

### tags 为空数组

`status: "done"` + `tags: []` 表示**本次打标成功执行，但没有产出任何标签**。
这是正常结果，不是错误——通常是企业信息太少（只有名称、没有经营范围）导致的。

---

## 11. 容量与运维

| 项 | 当前值 |
|---|---|
| 调用频率上限 | 默认 **60 次/分钟**（按 app_id 计），可按需上调，联调前告诉我们你的预期 |
| 429 响应 | 带 `Retry-After` 响应头（秒）与响应体里的 `retry_after`，按它退避即可 |
| 并行任务数 | **不限**，同时存在多个 `running` 任务没有问题 |
| 单次上送 | 单片 ≤ 500 家；任务总量不限，我方内部自动拆批 |
| 单页响应体 | `limit=200` 时约 1 MB（按平均每家 18~19 个标签估算） |
| gzip | **支持**，带 `Accept-Encoding: gzip` 即可；小于 1KB 的响应不压缩 |
| 结果保留 | **长期保留，不做自动清理**，任务完成后可无限期重复拉取 |
| 计划内维护 | 返回 `503`，请按退避重试；我们会提前通知窗口 |

---

## 12. 常见问题

**任务能中途取消吗？**
不能。当前版本不开放任务控制，需要中止请联系我方人工处理。

**同一家企业重复提交会怎样？**
按 `external_id` 识别为同一家，会复用企业档案并**重新打标**。
结果里**只返回本次任务产出的标签**，历史标签不重复回传——请在你方自行累积。
我方保留每个标签来自哪个任务、什么时间、依据什么经验，需要追溯时可以提供。

**上送的企业信息会被修改吗？**
不会。你方送来的工商信息在我方是只读留档，我们只在其基础上产出标签，不回写、不修改。

**结果能重复拉取吗？**
可以，无限次。已产出的结果不会变化，同一个 `cursor` 永远返回相同内容。
