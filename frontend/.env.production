# 页面标题
VITE_APP_TITLE=知识库管理
# 接口前缀：正式环境前端与后端同域（platform-enterprise.yicall.com），
# nginx 已配置 /kb-api 反向代理到后端并剥前缀（见 deploy/nginx.conf.example、运维.md）。
# 用相对路径而非写死域名，换域名/加环境时不需要改前端代码重新打包。
VITE_APP_BASE_API=/kb-api

# ---- 统一身份认证（issue #37/#38） ----
# 联调上线（issue #38，平台登记参数就绪）时切换为 unified 并放开下面两行；
# 在此之前保持 off，线上行为与接入前一致。后端 .env 的 AUTH_MODE 需同步切换。
VITE_AUTH_MODE=off
# VITE_AUTH_MODE=unified
# VITE_IDENTITY_LOGIN_URL=https://platform-identity.yicall.com/web/
# VITE_AUTH_DOMAIN=PUBLIC
