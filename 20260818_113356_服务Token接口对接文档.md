# 服务 Token 接口对接文档

## 一、变更概览

统一身份平台新增服务 Token 获取和校验接口，供无用户上下文的系统间调用使用。

- 服务 Token 与用户 `IDENTITYTOKEN` 完全隔离。
- 服务 Token 只绑定来源系统和目标系统，不返回权限范围。
- 业务系统使用 `X-Service-Token` 向目标系统传递服务 Token。
- 来源系统和目标系统均须先登记为统一身份平台的业务系统，并配置客户端密钥。
- 统一身份平台不维护系统间调用授权关系，启用系统之间默认可以申请服务 Token。

## 二、公共签名规则

两个接口均使用六段式 HMAC-SHA256 签名。

### 2.1 请求头

| 请求头 | 必填 | 说明 |
|---|---|---|
| `X-Sso-Client` | 是 | 当前调用方的系统编码 |
| `system-code` | 是 | 当前调用方的系统编码，必须与 `X-Sso-Client` 一致 |
| `X-Sso-Timestamp` | 是 | 当前 Unix 毫秒时间戳，服务端允许前后5分钟偏差 |
| `X-Sso-Nonce` | 是 | 每次请求唯一的32位十六进制随机数 |
| `X-Sso-Signature` | 是 | 64位十六进制 HMAC-SHA256 签名 |

### 2.2 签名原文

```text
HTTP_METHOD
REQUEST_PATH
X_SSO_CLIENT
TIMESTAMP
NONCE
SHA256(CANONICAL_JSON_BODY)
```

使用客户端密钥对上述原文计算 HMAC-SHA256，结果输出为小写十六进制字符串。

请求体摘要使用 UTF-8 编码的紧凑 JSON。字段顺序固定：

- 获取 Token：`{"sourceSystem":"bqxt","targetSystem":"knowledge"}`
- 校验 Token：`{"serviceToken":"实际Token"}`

不要在签名 JSON 中添加空格、换行或调整字段顺序。

获取 Token 的签名原文示例：

```text
POST
/core/service-auth/token
bqxt
1787000000000
0123456789abcdef0123456789abcdef
<请求体的SHA-256十六进制摘要>
```

## 三、新增接口

### 3.1 获取服务 Token

来源系统使用自身客户端密钥申请面向单一目标系统的短期 Token。

- 请求方式：`POST`
- 接口路径：`/core/service-auth/token`
- Content-Type：`application/json`

请求体：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `sourceSystem` | String | 是 | 来源系统编码，必须与两个系统请求头一致 |
| `targetSystem` | String | 是 | Token 唯一允许访问的目标系统编码 |

```json
{
  "sourceSystem": "bqxt",
  "targetSystem": "knowledge"
}
```

完整成功响应：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "serviceToken": "随机服务Token",
    "sourceSystem": "bqxt",
    "targetSystem": "knowledge",
    "expiresIn": 7200,
    "expiresAt": 1787007200000
  }
}
```

| 返回字段 | 类型 | 说明 |
|---|---|---|
| `serviceToken` | String | 业务调用时放入 `X-Service-Token` 的原始 Token |
| `sourceSystem` | String | Token 绑定的来源系统 |
| `targetSystem` | String | Token 绑定的目标系统 |
| `expiresIn` | Long | 有效秒数，当前固定为7200秒 |
| `expiresAt` | Long | Unix 毫秒过期时间戳 |

签发校验：

- `X-Sso-Client`、`system-code`、`sourceSystem` 必须完全一致。
- 来源系统和目标系统必须存在且处于启用状态。
- 统一身份平台不判断来源系统是否允许访问目标系统的具体接口。

### 3.2 校验服务 Token

目标系统使用自身客户端密钥校验收到的服务 Token。

- 请求方式：`POST`
- 接口路径：`/core/service-auth/verify`
- Content-Type：`application/json`

请求体：

| 参数 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `serviceToken` | String | 是 | 从业务请求 `X-Service-Token` 中取得的原始 Token |

```json
{
  "serviceToken": "随机服务Token"
}
```

有效 Token 响应：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "active": true,
    "sourceSystem": "bqxt",
    "targetSystem": "knowledge",
    "issuedAt": 1787000000000,
    "expiresAt": 1787007200000
  }
}
```

无效 Token 响应：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "active": false
  }
}
```

出现以下任意情况时统一返回 `active=false`，不暴露具体失败原因：

- Token 不存在或已经过期。
- Token 记录内容不可解析。
- Token 绑定的目标系统与当前 `X-Sso-Client` 不一致。
- 来源系统或目标系统已经停用。

## 四、业务系统处理建议

### 4.1 来源系统

1. 使用 HMAC 调用获取 Token 接口。
2. 只在服务进程内存中缓存 `serviceToken` 和 `expiresAt`。
3. 调用目标系统时携带：

```http
X-Service-Token: <serviceToken>
```

4. Token 到期前主动重新获取；业务调用收到401后强制获取一次并仅重试一次。
5. 不要把服务 Token 放入 `IDENTITYTOKEN` 请求头。

### 4.2 目标系统

1. 从业务请求读取 `X-Service-Token`。
2. 使用目标系统自身客户端密钥调用校验接口。
3. 对高频调用场景，建议对 `active=true` 的校验结果进行本地缓存，减少对统一身份平台校验接口的重复调用。缓存有效期不得超过 Token 的剩余有效期，并应设置合理的较短上限，以兼顾性能和系统停用后的生效时效。
4. 仅在 `active=true` 且 `targetSystem` 等于自身系统编码时接受身份。
5. 根据 `sourceSystem` 和目标系统自己的接口白名单控制可访问接口。
6. 统一身份平台不维护系统间授权关系，目标系统必须自行判断来源系统是否有权访问当前接口。
7. 所有子系统对外提供的接口均须进行鉴权。鉴权入口应优先识别并校验系统侧凭证，再判断是否进入用户侧鉴权；携带 `X-Service-Token` 的请求不得被用户 SSO 拦截层提前拒绝。
8. 仅携带服务 Token 时执行系统侧鉴权；仅携带用户 Token 时执行用户侧鉴权；两类 Token 均未携带时拒绝请求。
9. 如果业务请求同时携带用户 Token 和服务 Token，应拒绝请求，避免身份混用。

## 五、错误处理

| 场景 | HTTP 状态 | 处理建议 |
|---|---:|---|
| 请求头缺失或参数校验失败 | 400 | 修正请求参数，不重试 |
| HMAC 参数、时间戳、签名或客户端凭证无效 | 401 | 检查密钥、时钟和签名原文，不盲目重试 |
| 系统编码不一致或目标系统未启用 | 403 | 检查系统登记、启用状态和请求参数 |
| 被校验 Token 无效 | 200，`active=false` | 目标系统拒绝本次业务请求 |

## 六、存储与安全注意事项

- Redis Key 格式：`SERVICE_AUTH:TOKEN:<serviceToken>`。
- Redis Value 记录 `sourceSystem`、`targetSystem`、`issuedAt` 和 `expiresAt`。
- Redis TTL 与 Token 的2小时有效期一致。
- Token 不进行 SHA-256 摘要转换。
- 日志、异常信息、链路标签中禁止记录客户端密钥、完整签名或原始服务 Token。
- nonce 在签名通过后写入 Redis，有效期5分钟；同一客户端不得重复使用 nonce。
